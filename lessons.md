# lessons — 자가학습 오답노트

> `autoeval_hook.py` 가 실패 신호를 감지하면 Claude 가 스스로 여기에 1건씩 append 한다.
> 사람이 직접 고치거나 지워도 된다(**저장은 자동, 삭제는 수동**).
>
> **트리거** 키워드가 다음 프롬프트에 들어 있으면 그 교훈만 자동으로 컨텍스트에 다시 주입된다.
> 전부 주입하지 않는다 — 안 걸리면 0토큰.

## 이 파일 보는 법 (2026-07-23 재정리)

교훈은 **적용 범위별 3구역**으로 나뉜다. 지금 하는 작업에 맞는 구역 + 공통만 보면 된다.

| 구역 | 언제 보나 | 건수 |
|---|---|---|
| 📌 공통 — 모든 작업에 적용 | 어떤 폴더에서 무슨 일을 하든 먼저 본다. 일하는 방식·검증 태도. | 13건 |
| 💻 개발 — 코드·git·자동화·PC 운영 | v_up(위젯·할일)·mail·walk 등 코드를 고칠 때 본다. | 6건 |
| 📄 문서작성 — 사업계획서·이력서·신청서·HWPX·한글 | auto_write 계열 문서 산출물을 만들 때 본다. 코드 작업에는 해당 없음. | 66건 |

- 전체 85건. 번호(L001~)는 **한 번 붙으면 바뀌지 않는다** — 다른 교훈·스킬이 번호로 서로를 참조하기 때문.
  그래서 구역이 달라도 번호는 뒤섞여 있다(구역 안에서만 번호순 정렬).
- 새 교훈은 `## L<번호> | <구역> | <YYYY-MM-DD> | <제목>` 형식으로 해당 구역 끝에 붙인다.
- 예전에 번호가 겹쳤던 3건은 2026-07-23 정리하며 L082·L083·L084 로 옮겼다(내용은 그대로).

---

# 📌 공통 — 모든 작업에 적용 (13건)

> 어떤 폴더에서 무슨 일을 하든 먼저 본다. 일하는 방식·검증 태도.

## L005 | 공통 | 2026-07-10 | 결과물 검증은 눈으로
- **트리거:** 검증, 재확인, 틀린것, 확인해, 검수
- **실수:** 코드 로직만 리뷰하고 "이상 없음"이라 보고했는데, 실제 렌더링본에서 29장 중 9장이 틀려 있었다.
- **재발방지:** 사람이 볼 산출물(PDF·문서·이미지)은 렌더링해서 **실제 픽셀을 읽어** 검증한다. 필요하면 병렬 에이전트로 페이지를 나눠 적대적으로 확인한다. 로직 리뷰는 검증이 아니다.

<!-- 아래 L006~L028 은 2026-07-11 auto_write 과거 기록(CLAUDE.md 변경이력·메모리·원장)에서 backfill 한 것. -->

## L026 | 공통 | 2026-07-11 | 파일 경로는 묻는다
- **트리거:** 경로, 파일위치, 폴더, 스캔, 어디
- **실수:** 파일 경로를 광역 Glob으로 스캔하려 했다.
- **재발방지:** 경로 자동 광역탐색 금지. 사용자에게 직접 물어본다.

## L027 | 공통 | 2026-07-11 | 버전규칙 + 반복질문 금지
- **트리거:** 버전, v1, v2, 반복질문, 이미말한
- **실수:** 산출물을 날짜로만 구분하거나, 사용자가 이미 말한 결정을 다시 물었다.
- **재발방지:** 산출물은 v1, v2로 버전 구분(날짜만 X). 이미 말한 결정·정보는 기억하고 다시 묻지 않는다.

## L029 | 공통 | 2026-07-11 | 규칙 충돌 시 최신 우선
- **트리거:** 규칙, 충돌, 최신, 버전, 갱신
- **실수:** 새 지시가 기존 규칙과 어긋나는데 옛 규칙을 그대로 따르거나, 중복 규칙을 신설했다.
- **재발방지:** 새 규칙이 기존과 충돌하면 **최신 버전으로 교체**한다(중복 신설 X). 충돌하는 옛 규칙은 lessons·메모리·위키 어디에 있든 최신으로 갱신한다.

## L030 | 공통 | 2026-07-12 | 사용자 지적은 최소 범위로만 수정
- **트리거:** 폰트 통일, 서식 통일, 여기만 다름, 전역, 광범위
- **실수:** "자격표만 폰트 다름" 지적에 전역 명조 통일(58건)로 과잉 변경 → 원래 HY그래픽 라벨까지 밋밋해짐(§1 최소변경 위반).
- **재발방지:** "여기만 다르다"류 지적은 그 '여기'만 주변과 맞춘다. 전역 sweep 금지. 변경 건수가 지적 범위보다 크면 멈추고 확인.

## L065 | 공통 | 2026-07-15 | "비교·학습해라" 요청 = 전수조사가 기본 — 하이라이트 몇 개로 끝내지 마라
- **트리거:** 비교, 학습해, 차이점, 대조, 전수
- **실수:** 사용자 문서 vs AI 문서 비교 학습 요청에 눈에 띄는 2건만 뽑고 끝냄 → "이게 다야? 전수조사" 지적. 전수 돌리니 검증된 차이 33건(교훈 25건)이 나왔다.
- **재발방지:** 비교·학습 요청은 차원 분해(섹션 구성/표 스키마/셀 값·문구/시각 요소/파일·폴더 관행) 후 차원별 병렬 대조 + 적대 검증으로 전수조사한다(Workflow 활용). 상위 N개 요약은 보고 형식일 뿐, 수집은 전수.

## L066 | 공통 | 2026-07-16 | 매 실질턴 전역 마무리 프로토콜 고정(누락 금지)
- **트리거:** 마무리, 정리멘트, 전역요청, RESUME, 방금 한 일, 프로토콜, 왜안하냐
- **실수:** 매 실질 답변에 요구되는 전역 마무리 중 🧾"방금 한 일" 요약과 RESUME.md 체크포인트 갱신(훅 요청)을 일부 턴에서 건너뛰어 사용자가 "전역요청사항 왜 안 하냐" 지적.
- **재발방지:** 파일수정·다단계·분석 등 실질 답변은 끝에 4종 고정 부착 — ①첫줄 상태표시 ②해결 Y/N(N이면 해결법) ③👉 다음 프롬프트 ④🧾 방금 한 일(before→after). 그런 턴엔 RESUME.md도 같은 턴에 갱신(보존식). 단답·순수설명·잡담만 예외.

## L068 | 공통 | 2026-07-20 | 역할 부여·페르소나 요청에 장황하게 응답 금지 — 역할은 행동으로 증명
- **트리거:** 역할, 페르소나, 넌 ~다, 신하, 직원, 왕, 대표, 말이많다, 짧게
- **실수:** 사용자가 "충실한 신하/거짓 없음" 역할을 지시했을 때 역할 수락 선언·다짐을 길게 서술 → "역할극 너무 말이많다 그냥 대표랑 충실한직원이라 하자"로 교정받음.
- **재발방지:** 역할·페르소나 지시는 **한 줄 수락 후 즉시 본 업무 착수**. 역할 준수는 말이 아니라 행동으로 보인다 — ①요청 범위 밖 작업 안 함 ②미검증을 완료라 하지 않음 ③되돌리기 어려운 작업 전 1줄 확인. 역할 재확인·다짐 문구 반복 금지.

## L073 | 공통 | 2026-07-23 | 사전 진단의 예측을 실측 전에 결론처럼 말하지 마라
- **트리거:** 진단, 예측, 기대효과, 추정, 클러스터, 재진단, 워크플로 분석
- **실수:** 병렬 진단 워크플로가 "낡은 판정을 재진단하면 10~30개 소스를 켤 수 있다"고 산출했고 이를 우선순위 1위로 보고했으나, 실제 전수 재진단 결과는 **0개**였다(뒤집힌 15건은 전부 이미 켜져 있던 소스). 서브에이전트의 접속 프로브(HTTP 200)와 실제 수집기의 파싱 성공은 다른 문제였다.
- **재발방지:** 서브에이전트·워크플로의 산출은 **가설**이지 결론이 아니다. "몇 개를 얻는다"는 수치 예측은 실측 후에만 사용자에게 말한다. 특히 "접속 가능"과 "실제 수집 가능"을 구분하고, 검증은 반드시 **운영 코드 경로**(여기선 monitor.fetch_html_generic)로 돌린다.

## L075 | 공통 | 2026-07-23 | 산출 방식을 바꾸면 이전 확정 속성이 조용히 사라진다 — 이월 체크리스트 필수
- **트리거:** 방식 전환, 재생성, 이전에 됐던, 회귀, 볼드, 안내문구 삭제, 폰트 위계, 재현본
- **실수:** 온랩 사업계획서를 DOCX 재현본 → 원본 붙임1 페이지 추출 방식으로 바꾸면서, 이전 판에서 이미 확정됐던 속성(소제목 볼드·안내문구/상자 삭제·폰트 위계)이 통째로 빠졌는데 아무도 못 알아챘다(사용자 발견).
- **재발방지:** 산출 방식·파이프라인을 전환할 때는 **직전 확정본과 시각·서식 속성을 항목별 대조**(볼드·크기·안내 삭제·이미지·표)해 이월 체크리스트를 만들고, 전환본에서 각 항목 유지 여부를 실측한다. "새 방식이 더 정확하다"는 전환 사유가 이전 확정 스타일의 폐기를 정당화하지 않는다.

## L077 | 공통 | 2026-07-23 | 산출물 재생성 때마다 '이전 버전엔 됐는데 지금은 안 되는 것' 회귀 전수점검 의무
- **트리거:** 재생성, 다시 만들, 버전, 제출본, 회귀, 서명, 사라졌, 이전 버전, 패키지
- **실수:** 제출본을 여러 번 재생성(v6→…→v12)하면서 각 회차의 새 수정만 확인하고 **직전 확정판의 기존 속성(서명·굵기·안내삭제 등)이 유지됐는지는 안 봤다.** 실제로 방식 전환 때 볼드·안내삭제가 소실됐고(L075), 서명 소실 의심 신고도 나왔다(이번엔 오인이었지만 점검 없이는 구분 불가).
- **재발방지:** 재생성 후에는 **직전 확정판 대비 회귀 전수점검을 의무**로 돌린다(사용자 의무화 2026-07-23). ①기계 게이트: `D:\auto_write\scripts\submission_regression_check.py`(페이지수·서명 이미지·필수값·금지값·zip 구성, exit 0/2) ②자동판정 불가 속성(굵기·정렬·서명 위치)은 크롭 렌더 눈검증(L005). 둘 다 통과해야 "완성"이라 말한다. lessons-audit-gate 스킬의 필수 단계.

## L079 | 공통 | 2026-07-23 | 신규버전 작성 시 이전버전은 '구버전' 폴더로 모은다 (작업 폴더에 최신만)
- **트리거:** 새 버전, vN 작성, 산출물 폴더, 버전 정리, 구버전, output 폴더
- **실수:** 산출물을 v13까지 개정하는 동안 이전 버전 파일(v1~v12 hwpx/PDF 22개)을 작업 폴더에 그대로 쌓아 둬 최신본 혼동 위험을 만들었고, 사용자가 정리 규칙을 직접 지시하게 했다.
- **재발방지:** 새 버전(vN)을 만들면 **이전 버전들(v1~vN-1)을 같은 위치의 `구버전` 폴더 하나로 이동**한다(삭제 금지, 이동만). 작업 폴더에는 최신 버전만 남긴다. 단, 버전 계보가 모호한(이름 체계가 다른) 파일은 임의로 옮기지 않고, 스크립트 기본 경로가 구버전을 가리키면 최신으로 갱신 후 이동한다. (사용자 지정 2026-07-23, 메모리 output-versioning-and-no-repeat·위키 v1-v2 동기화됨)

## L082 | 공통 | 2026-07-16 | "요청 전체 정리"는 커밋(완료분)만 보면 놓친다 — 보류·거절·계획만도 포함
- **트리거:** 요청 정리, 요구사항 총정리, 내가 요청한 거, 레포단위, 놓친 요청, 원장, ledger
- **실수:** walk 요청 원장을 git 커밋(=병합 완료) 위주로 만들어, 계획만/보류/거절된 요청(멀티provider 지도·출발지 POI주소·사진업로드·로드뷰·자동차내비 풀재설계·PR#8 블록기능)을 누락. 또 사용자가 walk에 원한 자가학습을 mail 레포로 잘못 리다이렉트.
- **재발방지:** "모든 요청 정리"는 커밋 외에 .omc/plans/*·open-questions.md·wiki(미구현/보류/검토 grep)·RESUME 이력블록·SESSION_RECAP까지 훑어 완료✅/부분🟡/보류⏳/거절❌을 모두 싣는다. 자가학습·자동개발 대상 레포는 사용자가 지목한 레포로 고정(임의 이전 금지).

---

# 💻 개발 — 코드·git·자동화·PC 운영 (6건)

> v_up(위젯·할일)·mail·walk 등 코드를 고칠 때 본다.

## L067 | 개발 | 2026-07-16 | 워크트리 커밋은 git add -A 금지 — 세션 아티팩트(.omc) 쓸림 + squash 복구 경로
- **트리거:** git add -A, 워크트리, .omc, 세션 아티팩트, force-push 차단, squash
- **실수:** walk 워크트리에서 `git add -A`로 커밋해 OMC 훅이 만든 `.omc/state/*` 세션 파일 5개가 PR 브랜치에 push됨. force-push 리라이트는 가드가 차단.
- **재발방지:** ①커밋은 항상 **파일 명시 add**(수정한 파일만) — 워크트리엔 훅이 만든 잡파일이 있다고 전제. ②이미 push된 잡파일은 `git rm -r --cached`+제거커밋 후 **squash 병합**으로 main 이력에서 배제(force-push 불필요). ③워크트리 생성 직후 `.git/info/exclude`에 `.omc/` 추가.

## L069 | 개발 | 2026-07-21 | Git Bash 에서 git show/cat-file 의 ref:path 콜론이 MSYS 경로변환으로 깨짐
- **트리거:** git show, cat-file, ref:path, 콜론, MSYS, Git Bash, ambiguous argument
- **실수:** `git show origin/master:.gitignore` 가 `origin\master;.gitignore` 로 바뀌어 "ambiguous argument" 로 실패. Git Bash(MSYS)가 `:` 를 경로 구분자로 오인·변환.
- **재발방지:** `ref:path` 형태 인자 앞에 `MSYS_NO_PATHCONV=1` 을 붙인다 (`MSYS_NO_PATHCONV=1 git show origin/master:.gitignore`). 또는 `git show "origin/master:./path"` 처럼 `./` 를 넣거나, `git -C <repo> ...` + 파이썬으로 우회. 한글 경로면 `git -c core.quotepath=false` 도 함께.

## L071 | 개발 | 2026-07-23 | 지표 이름만 보고 의미를 넘겨짚지 마라 — 실제 데이터로 확인
- **트리거:** 지표, 비율, 파싱률, 카운트, 필드명, 통계, 판정기준, 임계
- **실수:** mail P0 탐지에서 `posted_parsed_count`를 "날짜 파싱 성공 수"로 넘겨짚어 파싱률을 계산 → 실사용 검증에서 정상 사이트들이 전부 0~27%로 나와 P1 오탐 11건. 실제 의미는 "직전영업일에 게시된 공고 수"라서 오늘 새 공고가 없으면 0이 정상이었다.
- **재발방지:** 기존 필드를 판정 기준으로 쓰기 전에 **그 필드를 만드는 코드 한 줄을 직접 확인**한다(이름·주석 신뢰 금지). 판정 로직은 반드시 **실데이터로 1회 돌려 분포를 눈으로 본다** — 정상이어야 할 대상이 대량 경고로 나오면 임계가 아니라 지표 선택이 틀린 것이다.

## L072 | 개발 | 2026-07-23 | 휴리스틱 자동보정 도구는 사람이 배선한 설정을 덮어쓴다
- **트리거:** autofill, 자동보정, 셀렉터, 일괄, 휴리스틱, 덮어쓰기, --write
- **실수:** `autofill_selectors.py --write`가 사람이 정교하게 배선해 둔 구체적 셀렉터까지 휴리스틱 추론값으로 교체. K-SURE는 유효행 12건 → 0건으로 망가질 뻔했다(14건 중 8건이 개악).
- **재발방지:** 일괄 자동보정은 **실행 전 백업 → 실행 후 옛/새 값을 실측 지표로 1:1 대조 → 열등·동률이면 원복**을 한 세트로 돌린다. "이미 값이 있으면 건너뛴다" 가드가 없는 도구는 신뢰하지 말 것. 살아난 대상은 실제 결과(제목 등)를 눈으로 확인해 메뉴·뉴스 같은 쓰레기가 섞였는지 본다.

## L081 | 개발 | 2026-07-23 | 프로세스 검색 후 종료할 때 자기 자신(PID)을 반드시 제외
- **트리거:** Get-CimInstance Win32_Process, CommandLine -like, Stop-Process, 프로세스 재시작, 위젯 재시작, pwsh 검색
- **실수:** 위젯 재시작을 하려고 `CommandLine -like '*-File*cockpit_widget.ps1*'` 로 pwsh 프로세스를 찾아 Stop-Process 했는데, 그 검색어가 **내 명령 자신의 명령줄에도 들어 있어** 스스로를 죽였다(exit 255). 같은 실수를 직전에도 겪었다 — 그때는 조회만이라 '위젯 2개' 오알람으로 끝났지만, 이번엔 종료라서 명령이 중단됐다.
- **재발방지:** 프로세스를 CommandLine 으로 찾을 때는 **항상 `-and $_.ProcessId -ne $PID` 를 붙인다.** 조회든 종료든 예외 없이. 패턴도 `'*-File *스크립트명*'`(-File 뒤 공백)처럼 실제 실행 형태에 맞춰 좁힌다. 종료 대상이 1개일 것으로 예상되면 Stop-Process 전에 매칭된 PID 목록을 먼저 찍어 자기 PID가 섞였는지 눈으로 확인한다.

## L085 | 개발 | 2026-07-23 | 같은 계산식이 여러 파일에 복제돼 있으면 한 곳만 고치고 끝내지 마라
- **트리거:** 같은 로직, 복제 코드, 한 곳만 수정, 동일 계산, 카운트, grep 전수, 다른 파일에도
- **실수:** `selfcheck.py` 가 교훈 수를 `count("\n## ")` 로 세어 안내 제목까지 포함하는 버그를 고쳤는데, **똑같은 식을 쓰는 `stats.py` 는 안 고쳤다.** 감시견만 확인하고 "정확해졌다"고 보고했고, 뒤늦게 다른 이유로 grep 하다 발견했다.
- **재발방지:** 계산식·상수·패턴을 고치면 **그 문자열 자체를 즉시 전수 grep** 해서 같은 코드가 있는 파일을 모두 찾아 함께 고친다(`Select-String -Path <dir>\*.py -Pattern '<식>'`). 수정 후에도 같은 grep 을 다시 돌려 "옛 방식 0건"을 확인하는 것까지가 한 세트다. 한 파일에서 증상이 사라진 것은 버그가 없어진 증거가 아니다.

---

# 📄 문서작성 — 사업계획서·이력서·신청서·HWPX·한글 (66건)

> auto_write 계열 문서 산출물을 만들 때 본다. 코드 작업에는 해당 없음.

## L001 | 문서작성 | 2026-07-10 | HWPX 이미지 크기
- **트리거:** hwpx, hwp, 이미지, 그림, 크기, 폭
- **실수:** HWPX 이미지 폭을 임의 배율로 넣어 본문폭의 3배가 됐다. 눈으로 안 보고 숫자만 고쳤다.
- **재발방지:** 본문폭 = 48190 HWPUNIT(A4 59528 − 좌우 5669). `hp:sz` / `hp:orgSz` / `hp:imgRect` / `hp:rotationInfo` **4곳 전부** 같은 폭으로 세팅하고, 고친 뒤 PNG로 렌더링해 눈으로 확인한다.

## L002 | 문서작성 | 2026-07-10 | HWPX 편집 후 글씨 깨짐
- **트리거:** hwpx, 글씨, 깨짐, 흩어, 레이아웃, 겹침
- **실수:** HWPX XML을 고친 뒤 텍스트가 흩어져 보였다. 원인을 못 찾고 본문 내용을 의심했다.
- **재발방지:** `hp:linesegarray` 는 낡은 레이아웃 캐시다. XML 편집 후엔 전부 제거하면 한글이 재계산한다. 본문을 건드리지 마라.

## L003 | 문서작성 | 2026-07-10 | 한글 COM 서버 실행 실패
- **트리거:** 한글, hwp, 변환, 저장, permissionerror
- **실수:** 한글이 열려 있는 상태로 COM을 호출해 `서버 실행 실패` / 파일 잠금 `PermissionError` 를 반복해서 만났다.
- **재발방지:** COM 호출이나 HWPX 쓰기 **직전에 항상** `Stop-Process -Name Hwp -Force` 로 한글을 먼저 닫는다.

## L004 | 문서작성 | 2026-07-10 | 세금계산서 공급받는자 추출
- **트리거:** 세금계산서, 공급받는자, 상호, pdf, 추출
- **실수:** "우리 회사가 아닌 쪽"을 공급받는자로 골랐다. 공급자가 다른 회사인 건에서 전부 뒤집혔다.
- **재발방지:** 전자세금계산서 PDF는 공급자 블록이 먼저, 공급받는자가 두 번째다. `(법인명)` 매치의 **두 번째 값**을 쓴다. 이름으로 필터링하지 마라.

## L006 | 문서작성 | 2026-07-11 | 파란/예시색 → 검정
- **트리거:** 파란, 예시색, 안내문구, 검정, 유색
- **실수:** 양식의 파란 안내·예시 글씨를 그대로 두거나 색만 남겼다.
- **재발방지:** 본문은 검정(000000) 통일. 명시 유색 런만 검정으로, 흰색·auto·테마색은 보존. HWPX는 게이트가 색을 못 보니 HWPX 단에서 직접 처리.

## L007 | 문서작성 | 2026-07-11 | 개인정보 마스킹
- **트리거:** 마스킹, 성명, 대학, 직장, 블라인드
- **실수:** 마스킹 대상(성명·대학명·직장명)에 실명·실제 직장명을 넣었다.
- **재발방지:** 성명·생년월일·대학(원)명·직장명은 ○/* 마스킹. 양식 더미 `OO학 박사`는 마스킹 의도지 빈칸 아님 — 실명 넣으면 규칙 위반. 학위·전공·직업·수행업무만 기재.

## L008 | 문서작성 | 2026-07-11 | 폰트 크기 위계 (기본 표준, 2026-07-11)
- **트리거:** 글자크기, 폰트, 본문, pt, 제목, 중제목, 소제목, 각주, 표
- **실수:** 제목·본문·표·각주에 크기 위계 없이 한 크기로 두거나, 대제목까지 건드렸다. (구 규칙 "본문 10pt"는 폐기)
- **재발방지:** 서식 우선순위(2026-07-23 사용자 확정): **1순위 = 공고·양식 명시 기준**(작성요령의 크기·폰트·줄간격·분량), **2순위 = 사용자 지정 위계**(공고가 침묵하는 속성만). 사용자 위계 기본값 — **대제목(최상위)=기존 크기 유지(건드리지 마라)** / **중제목 16pt** / **소제목 14pt** / **본문 12pt** / **각주 10pt** / **표 내용 11pt**. 예: 공고가 "12pt" 명시면 소제목도 12pt(단 굵게 등 공고가 안 정한 속성은 2순위 적용). 온랩 2026 실사례 = 소제목 12pt+굵게 확정.

## L009 | 문서작성 | 2026-07-11 | 날조 0
- **트리거:** 날조, 수치, 확인필요, 지어내, 근거
- **실수:** 근거 없는 실적·수치("수백 건")를 지어냈다.
- **재발방지:** 없는 실적·수치·경력 금지. 모르면 `[확인필요]` 또는 공란. 수치엔 산출근거 병기, 무출처는 `[확인필요]`.

## L010 | 문서작성 | 2026-07-11 | 예비창업자 = 기업정보 공란
- **트리거:** 예비창업, 기업정보, 사업자번호, 공란
- **실수:** 법인·사업자 없는데 기업 항목을 억지로 채웠다.
- **재발방지:** 법인 없으면 기업명="예비창업자", 창업일·사업자번호·매출·고용·투자·산업재산권은 공란. 대표 개인정보(성명·연락처)만 기입.

## L011 | 문서작성 | 2026-07-11 | 양식 서식 보존
- **트리거:** 서식보존, run, 원본, 훼손, 채우기
- **실수:** 값을 채우면서 폰트·스타일·표 구조를 바꿨다.
- **재발방지:** python-docx는 `run.text`만 교체(폰트/스타일 객체 미변경). 안내문구 표·문단만 삭제. 원본 out==in이면 ValueError.

## L012 | 문서작성 | 2026-07-11 | 양식 안내문구 삭제
- **트리거:** 작성요령, 작성방법, 안내문구, 삭제
- **실수:** "작성방법/삭제 후 제출" 안내 표·단락을 남긴 채 제출본을 만들었다.
- **재발방지:** 안내문구 표·단락은 내용칸을 채운 **뒤** 제거한다(채우기 전 지우면 앵커 소실).

## L013 | 문서작성 | 2026-07-11 | 본문 메타 문구 금지
- **트리거:** 메타, 작성태도, 정직하게, 미확정
- **실수:** `※` 안내줄·"날조하지/과장하지/미확정" 같은 작성태도 문구를 본문에 남겼다.
- **재발방지:** 본문엔 실제 계획만. `※` 작성태도·자기지시 문구는 제거(strip_meta_notes).

## L014 | 문서작성 | 2026-07-11 | 표 헤더/내용행 서식 구분
- **트리거:** 표, 헤더, 음영, 서식, 셀
- **실수:** 생성한 표의 전 셀을 동일 서식으로 만들었다.
- **재발방지:** 첫 행=헤더(음영+굵게), 둘째 행부터=내용(무음영+보통). 전 셀 동일 서식 금지.

## L015 | 문서작성 | 2026-07-11 | ■ 제목 서식
- **트리거:** 제목, 소제목, 라벨, 구분자
- **실수:** "■ 라벨 — 설명" 형태로 뒀다.
- **재발방지:** "■ 라벨 — 설명"/"■ 라벨(부제)" → "■ (라벨)" 로 정규화(reformat_bullet_heading).

## L016 | 문서작성 | 2026-07-11 | 쉬운 표현
- **트리거:** 표현, 유행어, 쉽게, 용어
- **실수:** 유행어·난해 표현("라스트 100m")을 썼다.
- **재발방지:** 유행어·난해표현 금지. 쉽고 구체적으로. 비개발자가 바로 이해할 말로.

## L017 | 문서작성 | 2026-07-11 | 이미지 = NotebookLM 프롬프트
- **트리거:** 이미지, 그림, 차트, 프롬프트, 시각화
- **실수:** 그림 자리에 차트를 직접 생성했다.
- **재발방지:** 그림 위치엔 NotebookLM 슬라이드 프롬프트를 삽입한다(`▣ [그림 N] 제목 ▷ 프롬프트:"…"`). 차트 자동생성 X.

## L018 | 문서작성 | 2026-07-11 | 분량은 공고 기준
- **트리거:** 분량, 페이지, 공고, 몇장
- **실수:** 공고가 명시한 분량 제한을 무시했다.
- **재발방지:** 기본 10~15p, 공고가 제한/권장을 명시하면 그에 우선 맞춘다(예: AI인재 계획 2p 내).

## L019 | 문서작성 | 2026-07-11 | 점수 통과 ≠ 제출 가능
- **트리거:** 제출, 게이트, DRAFT, 수용검사, 품질점수
- **실수:** 품질점수 99.5 "통과"인데 실제로는 제출불가인 문서를 통과로 봤다.
- **재발방지:** 품질점수는 서식만 본다. 제출가능성은 usage_acceptance 게이트가 판정 — fail 결함 1개면 출력명에 `_DRAFT` 강제. self_diagnose exit 0=제출가능/2=제출불가.

## L020 | 문서작성 | 2026-07-11 | DRAFT 세탁 / fail-open 금지
- **트리거:** DRAFT, 세탁, fail-open, 검사예외, 이름
- **실수:** 수용검사가 예외로 죽으면(검사불능) DRAFT 마킹을 건너뛰어 제출불가 문서가 깨끗한 이름으로 유출됐다.
- **재발방지:** 검사 예외·판정불가 시 fail-closed — `_DRAFT` 강제 + needs_input. 최종 복사·이름 승격 전에 draft_marked/acceptance_error를 확인. clean 산출물도 fail 분기 artifacts에 포함.

## L021 | 문서작성 | 2026-07-11 | lxml proxy id 재사용 dedup 버그
- **트리거:** dedup, 셀, proxy, 과소집계, lxml
- **실수:** 표 셀 순회 dedup을 `id(cell._tc)`로 했더니 proxy 소멸 시 id 재사용으로 다른 셀을 '이미 봄'으로 오판, 과소집계.
- **재발방지:** 셀 dedup은 id() 쓰지 말고 refs 리스트로 proxy 생존을 유지한다(검출↔교정 셀 순회 정합). 검출기·교정기가 같은 dedup 로직 공유.

## L022 | 문서작성 | 2026-07-11 | 유색 정규화 auto값 오교정
- **트리거:** 유색, auto, 정규화, textColor, 검정
- **실수:** `val="auto"` 런을 hex로 오인해 검정으로 덮어썼다.
- **재발방지:** 검정 교정은 정규 6자리 hex(`[0-9a-f]{6}`)만. auto·테마색·미지정은 보존. 검출(color.rgb=None 통과)과 교정을 역연산으로 정합. 머리글/바닥글/텍스트박스·하이퍼링크 안 런도 순회.

## L023 | 문서작성 | 2026-07-11 | cross-form 이름 필드 오매칭
- **트리거:** cross-form, 대표자명, 이름, 전사, 자동채움
- **실수:** '대표자명' 칸에 역할서술("기술개발, 특허전략 총괄")을 high로 자동전사했다.
- **재발방지:** 이름 동의어 클러스터 타깃은 값이 이름 모양(20자 이하·콤마/및/총괄/담당 없음)이 아니면 강등→needs_confirm. 오매칭 < 빈칸.

## L024 | 문서작성 | 2026-07-11 | 플레이스홀더 빈칸 승격 (O마스크 보존)
- **트리거:** 플레이스홀더, 예시, 빈칸, 채움, 마스크
- **실수:** 예시값(`2000.00.00`·`000억원`) 든 칸을 '이미 채워짐'으로 보고 못 채웠다 / 또는 O마스크를 placeholder로 오인해 실값을 덮었다.
- **재발방지:** 불가능날짜·전부-0수량·더미등록번호만 빈칸 승격. **O마스크(OOO)는 GOOGLE/O2O 같은 실단어일 수 있어 제외**. 실값은 절대 덮어쓰기 금지(2중 게이트).

## L025 | 문서작성 | 2026-07-11 | 체크박스 부분문자열 오체크
- **트리거:** 체크박스, 선택칸, 정확일치, 옵션
- **실수:** □→■ 체크를 부분문자열로 매칭해 "개인정보보호"→개인, "00법인"→법인을 오체크했다.
- **재발방지:** 정규화 후 **정확일치만** 체크. 부분문자열·짧은값·예시값 오체크 금지. `.//w:r` 순회로 하이퍼링크 □도 처리.

## L028 | 문서작성 | 2026-07-11 | 본문 폰트 전역통일 도구
- **트리거:** 폰트통일, 글꼴, 전역, 글자체
- **실수:** 단락별 서식통일 함수로 본문 전역 글꼴을 맞추려 했다(전역통일 불가).
- **재발방지:** 본문 전역 글꼴·크기 통일은 `scripts/unify_body_font.py`(제목위계 보존, 범용). `doc_quality_ops.unify_paragraph_formatting`은 단락별이라 전역통일 못 함.

## L031 | 문서작성 | 2026-07-12 | 한글/HWPX 시각문제는 추측패치 금지 — 원인 특정 먼저
- **트리거:** 표, 선, 괘선, 폰트, 겹침, 자간, 이상해, 렌더, 한글 화면, 사진칸
- **실수:** 한글 렌더를 직접 못 보면서 시각 문제(줄바꿈·선 사라짐·폰트)를 추측으로 여러 번 패치 → 담당업무 열이 더 나빠지는 등 반복 악화. 사용자가 defect를 계속 지적하게 만듦.
- **재발방지:** 시각 문제는 ①정상 원본(이미지/파일) 확보 ②XML 정밀 진단(rowSpan·borderFill·격자 타일링 검증)으로 원인 1개를 특정한 뒤 그 부분만 최소 수정. 원인 특정 전 전역 sweep·추측 수정 금지, 렌더 확인 전 "완료" 보고 금지.

## L032 | 문서작성 | 2026-07-12 | 서명일·날짜 기입은 실행 시점 시스템 날짜로
- **트리거:** 서명일, 날짜, 00월, 00일, 제출일, 오늘
- **실수:** 신청서 서명일을 대화 맥락의 낡은 날짜(11일)로 하드코딩해 실제 날짜(12일)와 어긋남 → 사용자가 정정.
- **재발방지:** 문서의 서명일·작성일 채움은 항상 실행 시점 시스템 날짜(datetime.now)로 자동 산출한다. 대화·RESUME에 적힌 날짜를 복사하지 않는다. 신청서 완성 절차(서명 삽입)와 한 세트로 실행.

## L033 | 문서작성 | 2026-07-13 | 파일 유효성 ≠ 앱에서 열림 / 체크리스트만 주기
- **트리거:** 안열림, hwpx, 한글, 불러오기, 파일손상, 열기실패
- **실수:** hwpx가 zip/XML상 유효하니 "정상"이라 단정하고, 사용자에게 "닫고 다시 열어보라"는 체크리스트만 반복 제시(직접 안 고침). 실제 원인은 한글 앱/자동화 엔진 비정상이었다.
- **재발방지:** "안 열림" 보고 시 구조검증에 그치지 말고 ①실제 앱(COM)으로 열림을 테스트해 파일 vs 앱 문제를 분리하고 ②사용자에게 시키지 말고 즉시 대안(한글 없이 열리는 docx/pdf 변환 등)을 만들어 준다. COM이 모든 호출 DISP_E_BADCALLEE·Version 공백이면 파일이 아니라 한글 앱 문제로 판정하고 재부팅/재활성화를 안내한다.

## L034 | 문서작성 | 2026-07-15 | 신청서 복수선택 칸은 임의 기본값 금지 — 사용자 확정
- **트리거:** 신청서, 체크박스, 가능업종, 분야, 복수선택, 2개 선택
- **실수:** 찾아가는1:1 컨설턴트 신청서에서 "세무·회계는 전 업종 공통"이라는 논리로 가능업종 5개 전부 ■ 처리, 분야도 1개만 체크. 정답(사용자 수정): 업종은 서비스·제조업·기타만 ■(음식업·도소매 □ 해제), 분야는 최대 2개 허용을 살려 세무·회계+특허 2개.
- **재발방지:** 체크형·복수선택 칸은 "전부 체크"나 "1개만" 같은 임의 기본값을 깔지 말고, 허용 개수(최대 N개)를 명시하며 사용자에게 선택지를 물어 확정한다. 논리적 그럴듯함(전 업종 공통)은 정답이 아니다.

## L035 | 문서작성 | 2026-07-15 | 키워드/전문영역 칸 = 분야명 반복 금지, 실전 강점 명사로
- **트리거:** 키워드, 전문영역, 예시, 신청서
- **실수:** 키워드칸에 신청 분야명(세무·회계)을 그대로 반복 기입. 정답(사용자): ①정책자금·정부지원금 ②자금조달·신용관리 ③손익분석 ④회계/재무 — 분야명이 아니라 본인 최강 전문영역 구체 명사.
- **재발방지:** 키워드·전문영역 칸은 분야명 복제가 아니라 그 사람의 실전 강점 키워드로 채운다. 박다솜 1순위 키워드 = 정책자금·정부지원금(저축은행 여신+정책자금 컨설팅이 최강점).

## L036 | 문서작성 | 2026-07-15 | 자기기술서(정성평가)는 다짐·수사 대신 '구체 성공사례 1개' 중심
- **트리거:** 자기기술서, 자기소개서, 정성평가, 지원동기, 각오
- **실수:** 5단 구성(지원동기/현장이해/역량/차별점/각오)에 수사적 표현("숫자의 사각지대")과 다짐 문단으로 작성. 정답(사용자): ①경력 1줄 요약 ②역량·실적 ③구체 성공사례("부결된 정책자금을 승인으로!" — 부결 원인 역산→체납 분할납부·연체 정리·지역신보 경로 변경 3단계→승인·폐업 방지)로 압축, 다짐은 마지막 1줄만.
- **재발방지:** 자기기술서는 문제→조치(구체 단계)→결과(정량)의 실제 성공사례 1개를 몸통으로 쓴다. 사례를 내가 지어내지 말고 사용자에게 "대표 성공사례 1개"를 먼저 물어본다. 지원동기·각오·수사는 최소화.

## L037 | 문서작성 | 2026-07-15 | 제출본 = 요구 서식만 분리한 별도 파일 (공고 전문에 채우기 X)
- **트리거:** 제출본, 신청서, 서식, 공고, 제출서류, 파일명
- **실수:** 공고 전문(모집요강+서식1~5)이 든 원본 파일 통째에 값만 채워 제출본이라 함. 정답(사용자): 필수 서식(신청서·자기기술서·청렴서약서)만 남긴 별도 파일 + 파일명 "서식명_성명.hwp" + 공고폴더/제출/ 하위에 PDF(이메일 제목 규칙과 동일한 이름)·증빙(경력·자격증·실적) 세트 구성. 학력칸도 최고학력(석사)만 남김.
- **재발방지:** 제출본 생성 시 ①요구 서식 페이지만 추출·분리 ②파일명=서식명_성명 ③제출 폴더에 본문+PDF+증빙 세트 ④신청서 학력 등 좁은 칸은 최고학력만 간결히. 서명은 저장된 서명 PNG 삽입까지 제안.

## L038 | 문서작성 | 2026-07-15 | 이력서 실적 표는 정량 컬럼(업체수·합계·회차)이 몸통 — 빼먹지 마라
- **트리거:** 이력서, 실적, 컨설팅 실적, 강의, 표, 프로필
- **실수:** 통합 이력서를 만들며 기간·기관·내용만 남기고 사용자 기존 이력서의 정량 컬럼을 뺐다 — 컨설팅 실적의 '업체수' 컬럼+'합계 행'(예: 합계 140개사), 강의의 '회차/시간'·'구분(민간/기관)', 주관기관 "공단 / (운영기관)" 병기.
- **재발방지:** 박다솜 이력서 실적 표 스키마 = 수행기간|구분|내용|**업체수**|주관/(운영)기관 + **합계 행**, 강의 = 일시|기관|주제|**회차/시간**|**구분(민간/기관)**. 기존 이력서(연세대 계열)의 표 스키마를 그대로 계승하고 숫자를 임의로 단순화하지 않는다. 철학: "숫자와 증거로 말한다".

## L039 | 문서작성 | 2026-07-15 | 이력서 끝은 포트폴리오(실물 이미지 증빙) — 텍스트로만 끝내지 마라
- **트리거:** 이력서, 포트폴리오, 이미지, 증빙, IR
- **실수:** 통합 이력서를 텍스트·표로만 구성하고 사용자 이력서의 마지막 '8. 포트폴리오' 섹션(IR자료 2종·추정 손익계산서·추정 매출액 등 실물 캡처 4점)을 통째로 뺐다.
- **재발방지:** 박다솜 이력서에는 끝에 포트폴리오 섹션(작업 산출물 이미지)을 유지한다. 원본 이력서에서 이미지 자산을 추출·이월하거나, 불가하면 "[포트폴리오 이미지 삽입 필요]" 자리를 명시하고 사용자에게 알린다.

## L040 | 문서작성 | 2026-07-15 | 필수 서식(서약서 등)은 원문 전문 유지 + 날짜·성명만 기입 — 서식 누락 금지
- **트리거:** 서약서, 청렴, 윤리강령, 동의서, 서식, 지원서 패키지
- **실수:** 지원서 초안(DOCX)에 공고 필수 [서식3] 청렴서약서를 통째로 뺐다(서약·청렴 grep 0건) = 제출 탈락 결함.
- **재발방지:** 공고의 모든 필수 서식을 빠짐없이 포함하고, 보일러플레이트 조항은 한 글자도 안 고치고 날짜·성명 칸만 채운다(사용자 최종본: 서약서 원문 유지 + "2026. 7. 15." + "컨설턴트 : 박 다 솜"만 기입).

## L041 | 문서작성 | 2026-07-15 | 신청서 실적은 공고 적합 건만 선별 — 전량 최신순 나열 금지
- **트리거:** 주요 컨설팅 실적, 실적 기재, 신청서 실적, 실적 선별
- **실수:** 실적 27건을 최신순 전량 나열(상단이 투자유치 3건) — 소상공인 공고와 무관한 투자유치·수출·R&D 포함.
- **재발방지:** 실적 칸은 공고 주관기관·대상 적합 건만 선별(사용자: 27건 중 소상공인 실적 5건+총괄 1줄만 — 더 최신인 투자유치를 건너뜀). 적합성 > 최신성.

## L042 | 문서작성 | 2026-07-15 | 담당업무·분야 값은 공고 대상에 맞춰 재서술 — 원문 복사 금지
- **트리거:** 담당업무, 분야 기재, 값 전사, 공고 맞춤, 각색
- **실수:** 프로필 값을 글자 그대로 복사(«컨설팅·자문·멘토링», «기업금융(부동산·PF)») — '소상공인' 키워드 0회.
- **재발방지:** 전사 시 공고 키워드를 duty 값에 주입해 선택적 각색(사용자: «소상공인·중소기업 컨설팅·자문·멘토링», «…소상공인 여신심사», 교육사업 문맥에선 «강의 운영»). 관련 없는 행(IPO브릿지)은 그대로 둔다.

## L043 | 문서작성 | 2026-07-15 | 이력서 학력·경력·자격 = '2. 경력사항' 단일 섹션·단일 표 + 블록별 서브헤더
- **트리거:** 이력서, 경력사항, 학력 표, 자격 표, 표 헤더
- **실수:** '학력·경력'/'자격 및 면허' 두 섹션·두 표로 분할하고 헤더를 '학교/직장·기관명' 슬래시 합성 단일 헤더로 뭉갬 — 사용자 코퍼스에 슬래시 합성 컬럼명 0건.
- **재발방지:** 섹션명 '2. 경력사항' 고정, 단일 표 안에 '학력|기간|학교명|전공학과|학위' / '경력|기간|직장명|직위|담당업무' / '자격|발급일자|자격증명|발급번호|발급기관' 블록별 헤더행 재선언(좌측 구분 셀 세로 병합). ※'자격 별도 표' 사용자 지시가 있으면 그 지시가 우선.

## L044 | 문서작성 | 2026-07-15 | 표준 이력서 골격 고정 — 프로필형 섹션 혼합·'기타 이력' 삭제 금지
- **트리거:** 이력서 섹션, 섹션 순서, 기타 이력, 수행 프로젝트, 통합 이력서
- **실수:** '이력서' 제목 문서에 프로필 전용 섹션(위촉·실적총괄·평가심사)을 끼워 넣고 강의를 8번으로 밀고 '수행 프로젝트'를 '연구용역'으로 개명, '기타 이력' 섹션 삭제로 항목 3건+ 소실(정부지원사업 선정사례 다수·한국기술혁신학회 회원·희망리턴패키지 경영개선 진단 전문가).
- **재발방지:** 이력서 골격 = 1.인적사항→2.경력사항→3.강의 실적→4.수행 프로젝트→(5.컨설팅 실적)→논문 및 저서→(7.기타 이력). 위촉·실적총괄·평가심사는 모두의성장 프로필 전용. 표에 안 맞는 이력은 '기타 이력' 자유 단락으로 보존.

## L045 | 문서작성 | 2026-07-15 | 서명=실이미지, '(인)' 텍스트 추가 금지 — 기준일은 문서 머리
- **트리거:** 서명란, (인), 확인합니다, 기준일, 끝맺음
- **실수:** 문서 끝에 '2026.  07.  박 다 솜  (인)' 임의 추가 — 사용자 코퍼스 전체에 '(인)' 0건, 기준일을 서명줄로 옮김.
- **재발방지:** 프로필형=기준일 '2026.07.'은 제목 바로 아래, 끝은 '이상의 내용이 틀림없음을 확인합니다.'+성명만. 표준 이력서=끝맺음 블록 자체가 없음. 서명 위치엔 서명 PNG 실삽입, 양식의 '(서명/인)' 문구는 유지.

## L046 | 문서작성 | 2026-07-15 | '~과정 이수'(교육 수료)는 자격증란 기재 금지
- **트리거:** 보유자격증, 자격 및 면허, 과정 이수, 교육 수료
- **실수:** '투자심사역 과정 이수'를 자격 표에 포함(AI 문서 2/2 위반).
- **재발방지:** '~과정 이수'는 [자격/교육 수료] 전용 섹션이 있으면 거기로, 없으면 생략(사용자 4개 문서 전부 자격란엔 미기재). ※국가/민간 구분 아님 — 사용자는 국가·민간 자격 혼재 기재.

## L047 | 문서작성 | 2026-07-15 | 신청서 주요경력 2칸 매핑: 왼쪽='기관 직위 (기간)' 합성, 오른쪽=담당업무
- **트리거:** 주요경력 칸, 경력 전사, 신청서 셀 매핑
- **실수:** 이력서 5열 서식('경력|기간|직장|직위|담당') 그대로 부어 신청서 2칸 구조 재조합 규칙 부재.
- **재발방지:** 왼쪽 셀 "밸류업파트너스 대표 (2022.11~현재)" 순 합성 + 오른쪽 셀 담당업무만(공고 맞춤 재서술 L042 동시 적용).

## L048 | 문서작성 | 2026-07-15 | 최종 제출 = 공고 명명 튜플로 개명한 단일 합본 PDF까지
- **트리거:** 최종 제출, 제출 파일명, PDF 합본, 이메일 제목 규칙
- **실수:** '제출본_v1.1.hwpx' 내부 작업명에서 종료 — 개명·PDF 합본 단계 없음(사용자가 수동으로 '(경기인천)_(세무·회계)_(박다솜)_컨설턴트 지원서.pdf' 68쪽 합본 생성).
- **재발방지:** 마지막 단계에 '공고 지정 튜플 개명 + 증빙 포함 단일 PDF 합본'을 넣는다. 공고의 이메일 제목 규칙을 파일명에도 적용.

## L049 | 문서작성 | 2026-07-15 | 산출물은 'YYYYMMDD 공고명' 폴더 + '제출/' 하위폴더에 배치
- **트리거:** 산출물 저장, 공고 폴더, 제출 폴더, 파일 정리
- **실수:** 신청서·사업계획서 산출물을 D:\auto_write git 루트에 방치(STAR_*, 국방기술* 등).
- **재발방지:** 공고 1건=폴더 1개('YYYYMMDD 공고명', 결과 확정 후 '- 합격/불합격' 태그 후기입), 실제 제출 세트는 내부 '제출/' 폴더 격리, 초안·공고문은 상위 유지. 산출물은 이 체계에 저장.

## L050 | 문서작성 | 2026-07-15 | 제출 최종본·증빙은 동일 파일명 HWP+PDF 쌍 병행 생성
- **트리거:** 최종본 확정, 제출용 변환, PDF 병행
- **실수:** 산출물 전부 HWPX/DOCX 단독 — PDF 쌍 0건(사용자 관행은 동일명 .hwp/.pdf 쌍 7건+).
- **재발방지:** 최종 제출본·증빙 확정 시 동일 파일명 PDF를 병행 생성(초안엔 불필요).

## L051 | 문서작성 | 2026-07-15 | 제출처 기관이면 정식 명칭으로 격상(조건부)
- **트리거:** 기관명 표기, 소진공, 약칭, 정식 명칭
- **실수:** 소진공 제출 문서에 «소진공(마켓링크)»·«소상공인진흥공단»('시장' 누락) 약칭 유지.
- **재발방지:** 제출처 기관명은 정식 명칭(소상공인시장진흥공단)으로 격상 — 단 내부 문서·타기관 제출에선 사용자도 약칭 사용(항상 아님, 조건부).

## L052 | 문서작성 | 2026-07-15 | 부문 체크박스를 분야 텍스트에 병합 금지 + 원문 구분자(·) 변조 금지
- **트리거:** 전문분야, 경영부문, 체크박스 행, 구분자
- **실수:** 경영부문 체크박스 행을 분야 셀 꼬리 '(마케팅·재무/자금…)'로 병합하고 '재무·자금'을 '재무/자금'으로 변조.
- **재발방지:** 부문(체크박스)과 분야(텍스트)는 별도 2행 유지, 원문 구분자 표기를 그대로 복제.

## L053 | 문서작성 | 2026-07-15 | 실적 없는 섹션도 삭제 말고 '해당사항 없음' 행으로 유지
- **트리거:** 빈 섹션, 실적 없음, 해당사항 없음
- **실수:** 프로필 파생본에서 [면접관] 섹션만 통째 생략.
- **재발방지:** 고정 양식 채움 시 실적 없는 섹션도 표를 유지하고 '해당사항 없음' 기입(사용자는 빈 양식에 없던 행을 직접 추가해서라도 유지).

## L054 | 문서작성 | 2026-07-15 | 실적 총괄 요약표는 세로형(라벨-값 행) — 가로 전치 금지
- **트리거:** 실적 총괄, 요약표, 표 방향
- **실수:** 세로 라벨-값 4행+병합 헤더를 가로 2x4로 전치, '컨설팅/멘토링 수행실적 합계' 병합 헤더 소실.
- **재발방지:** 소스 표의 방향·병합 헤더를 보존한다(임의 전치 금지).

## L055 | 문서작성 | 2026-07-15 | 소속기관 표: 1인기업 대표는 자기 회사로 전부 채움
- **트리거:** 소속기관, 기관 대표자, 기관 전화, FAX
- **실수:** 기관 대표자·주소·전화·FAX 매핑 규칙 부재로 [확인필요] 남발.
- **재발방지:** 기관명=밸류업파트너스, 대표자=박다솜(본인), 주소=사업장, 전화=휴대폰 재사용, FAX='-'(공란 아닌 명시) — 소스에 없어도 유도 가능.

## L056 | 문서작성 | 2026-07-15 | 신청서 단일 '주소' 칸 = 사업장 주소(거주지 아님)
- **트리거:** 주소 칸, 거주지, 사업장 주소
- **실수 위험:** 라벨 유사도로 '주소(거주지)'를 고를 뻔함.
- **재발방지:** 단일 주소 칸은 사업장 주소로 채우고 소속기관 주소와 통일(사용자는 거주지를 알면서도 사업장 기재).

## L057 | 문서작성 | 2026-07-15 | 한글 COM 전체실패는 재시도 말고 RESUME/다른 세션 기록부터 — 즉시 non-COM 우회
- **트리거:** 한글, COM, hwp, hwpx, BADCALLEE, 호출 수신자, 환경 고장, 재진단, 다른 세션
- **실수(안티패턴):** 한글 COM이 모든 호출에서 BADCALLEE로 실패하는데 kill·gen_py삭제·동적dispatch·재시도를 6턴 넘게 반복하며 시간낭비. RESUME.md에 "오늘 이 PC COM 불치 + 우회로(한글 GUI로 HWPX 저장→non-COM 좌표채움)"가 이미 기록돼 있었는데 늦게 확인.
- **재발방지:** 다중 세션 환경에서 한글/COM/환경 고장 증상이 나오면 **재시도·재진단 전에 RESUME.md·최근 세션 기록부터 확인**(이미 진단·우회로가 있을 수 있음). COM이 2회 연속 전체 호출 실패(BADCALLEE/서버예외)면 앱레벨 고장으로 확정하고 즉시 non-COM 우회(GUI 수동 HWPX 저장→좌표기반 hwpx_fill: set_cell_lines+□→■+linesegarray 제거)로 전환. (L003·L033 연장)

## L058 | 문서작성 | 2026-07-15 | 증빙은 granularity별 '모음/합본_날짜' 번들로 자산화·재사용
- **트리거:** 증빙 서류, 증명서 합본, 첨부 구성
- **실수:** 증빙 번들 생성·선택 단계 전무(사용자 수동 구성).
- **재발방지:** 공고 요구 증빙 → 기존 번들('증빙_전체합본_YYYYMMDD' 등) 선택 또는 공고 맞춤 신규 합본 생성 단계를 제출 플로우에 포함.

## L059 | 문서작성 | 2026-07-15 | 파일명 접미사=문서 '상태' — 작업 접미사·중간산출물로 제출 폴더 오염 금지
- **트리거:** 파일명, _converted, 접미사, 제출 폴더
- **실수:** 작업 서술형 접미사(_고도화·_완성·통합_v2·_converted) 사용, _converted 중간산출물이 사용자 제출/ 폴더에 잔존.
- **재발방지:** 사용자 규약 = 빈 양식 무접미사 / 완성본 '_박다솜' / 미완성 '작성필요' / 진행중 '(수정중)'. 변환 중간산출물은 제출 폴더에서 정리.

## L060 | 문서작성 | 2026-07-15 | 표준 이력서 강의 표엔 '구분'(민간/기관) 컬럼 유지
- **트리거:** 강의 실적 표, 강의 컬럼, 민간 기관
- **실수:** 강의 표를 3컬럼으로 축소해 구분(민간/기관) 값 소실.
- **재발방지:** 표준 이력서 강의 표 = 수행기간|주최기관명|강의 주제|회차/시간|구분 5컬럼(프로필형은 3컬럼 — 유형별 구분).

## L061 | 문서작성 | 2026-07-15 | 인적사항 표 사진 병합 칸엔 증명사진 항상 삽입
- **트리거:** 증명사진, 사진 칸, 인적사항
- **실수:** 사용자 표 구조(7x5)를 복제하고도 사진 칸을 빈 채 방치한 문서 존재(지원서 초안).
- **재발방지:** 이력서·신청서 생성 시 증명사진 삽입(파일: 증명사진 박다솜.jpg) 또는 [사진 삽입 필요] 명시.

## L062 | 문서작성 | 2026-07-15 | 번호 값의 라벨 접두어는 열 헤더 보고 결정
- **트리거:** 발급번호, 등록번호, 인증번호, 자격 표
- **실수:** '발급번호' 열에 «등록번호 12040»처럼 라벨 중복 기재.
- **재발방지:** 열이 '발급번호'면 번호만(«12040»), 열이 '비 고'면 라벨 병기(«등록번호 12040») — 열 이름이 값 서식을 결정.

## L063 | 문서작성 | 2026-07-15 | 자기기술서 골격(L036 보강): 번호 소제목 3단 + 성과 느낌표 헤드라인 + 공고 취지 호응 마무리
- **트리거:** 자기기술서, 소제목, 마무리 문장
- **실수:** 산문형 5단 구성으로 작성(사용자는 3단 번호 소제목으로 개편).
- **재발방지:** "1. 경력 → 2. 전문성 → 3. 컨설팅 성공 사례 : ~를 ~으로!"(성과 헤드라인) + 공고 사업명 인용 마무리("찾아가는 1:1 교육사업의 취지대로…").

## L064 | 문서작성 | 2026-07-15 | 제목·라벨 자간은 원본 그대로(공백 1칸) — 임의 2칸 금지
- **트리거:** 이력서 제목, 자간, 라벨 공백
- **실수:** '이  력  서'·'소  속'(2칸)으로 임의 확대 + 같은 표 안 '핸 드 폰'(1칸)과 불일치.
- **재발방지:** 사용자 표기 = '이 력 서'·'소 속'·'성 별' 전부 공백 1칸. 원본 자간 표기를 코드포인트 그대로 복제.

## L070 | 문서작성 | 2026-07-23 | 양식 고유 문구는 값 칸이 아니다 — 삭제·치환·색변경 전부 금지
- **트리거:** 양식, 안내문구, 예시, 파일명, 제출본, 채우기, 무가공
- **실수:** 온랩 참가신청서에서 "잘 보이게 한다"며 양식 안내문구(`* 아래 파란색 글씨는 예시입니다.`)를 지우고, 붙임서류 표의 빨간 파일명 예시(`팀명_대표자명_사업계획서`)를 실제 파일명으로 치환+검정 변경. 사용자 원칙 "양식은 조금이라도 다르게 보이면 안 된다" 위반. 사업계획서를 DOCX 재현본 양식으로 만든 것도 동일 위반.
- **재발방지:** 손대도 되는 곳은 **값이 들어갈 칸 안의 예시값뿐**(OOO·010-0000-0000 → 실값+검정). 안내문구·유의사항·파일명 예시·표 라벨은 색이 무엇이든 그대로 둔다. 재현본 제작 금지 — 항상 원본 파일에 채움. 제출 전 `diff_form.py`(원본↔작성본 전수 대조: 구조 카운트+텍스트 diff+charPr diff)를 게이트로 돌려 "양식 고유 영역 변경 0건"을 확인한다.

## L074 | 문서작성 | 2026-07-23 | linesegarray 전역 제거는 rhwp PDF 렌더를 깨뜨린다 — L002 는 '한글 전용'
- **트리거:** linesegarray, lineseg, rhwp, 겹침, 글자 겹침, 안내박스, 점선, PDF 변환, 표 셀
- **실수:** 온랩 hwpx 에서 L002("XML 편집 후 lineseg 전부 제거하면 한글이 재계산")를 전역 적용해 322개를 다 지웠다. 한글에서는 정상이지만 **rhwp 는 점선 안내박스(표 셀) 안 다중 문단의 줄배치를 lineseg 없이는 계산하지 못해 여러 줄을 한 줄에 겹쳐** 그렸고, 그 PDF 가 제출본에 들어갈 뻔했다(사용자가 발견).
- **재발방지:** ①lineseg 제거는 **편집한 문단만** — 손 안 댄 원본 문단의 lineseg 는 stale 이 아니라 정확한 값이므로 보존한다(rhwp 렌더 의존). ②rhwp 로 PDF 를 만들 파일은 전역 strip 금지 — 필요하면 lineseg 보존 계보에서 수정만 다시 적용해 파생한다(온랩 v4→v8 사례, `온랩\작업\make_v8_from_v4.py`). ③PDF 산출 후 안내박스·표 영역을 **크롭 렌더로 눈검증**한다(텍스트 추출로는 겹침이 안 보인다).

## L076 | 문서작성 | 2026-07-23 | HWPX charPr 신규 추가는 반드시 목록 맨 끝에 — 중간 삽입 시 스타일 전체가 밀린다
- **트리거:** charPr, 스타일 추가, header.xml, itemCnt, 서식 깨짐, 밑줄, 제목 서식
- **실수:** 볼드 소제목용 신규 charPr 를 기존 id 25 바로 뒤(중간)에 삽입 → id=인덱스 정렬이 깨져 위치 기반으로 해석하는 렌더러(rhwp)에서 id≥26 을 쓰는 모든 런의 서식이 한 칸씩 밀림(원본 제목이 밑줄·축소로 깨짐, 실측).
- **재발방지:** header.xml 에 charPr/paraPr 를 추가할 땐 **항상 목록 맨 끝에 append**(id=기존 최대+1, itemCnt 갱신)해 id==인덱스 정렬을 유지한다. 추가 후 원본 서식 영역(제목 등)을 렌더로 눈검증한다.

## L078 | 문서작성 | 2026-07-23 | HWPX 서명(이미지) 삽입 3함정 — 래퍼 표 앵커·inline 행잘림·PAPER 무시 + rhwp 알파는 렌더됨
- **트리거:** 서명 삽입, hp:pic, 떠 있는 개체, treatAsChar, 표 칸 이미지, 투명 PNG, BinData
- **실수:** ①"페이지 전체가 바깥 테두리 표 1칸"인 양식에서 '행 전체 검색'으로 앵커를 잡아 바깥 표 제목 문단에 삽입됨(페이지 상단에 렌더) ②표 칸에 글자처럼(treatAsChar=1) 넣으면 rhwp 가 행 높이로 잘라 획 일부만 보임(한글은 행이 늘어나 정상 — 뷰어별 차이) ③vertRelTo=PAPER 를 rhwp 가 무시하고 앵커+오프셋을 더해 그림(좌표 2배 이탈). 또 기존 기록 "rhwp 는 투명 PNG 미렌더"는 낡았음 — **v0.7.19 는 알파 PNG 를 정상 렌더**한다(구 기록 근거로 이중서명 설계하면 오판).
- **재발방지:** HWPX 에 서명을 넣을 땐 ①'값이 든 그 칸(tc)'을 직접 찾아 같은 행의 목표 칸을 앵커로 ②배치는 **칸 안 문단 앵커 + 떠 있는 개체(treatAsChar=0·글 앞·겹침 허용·PARA 기준)** — inline·PAPER 금지 ③삽입 후 흰배경판으로 rhwp 렌더 위치 눈검증 → 투명판 최종 ④"v(N-1) 렌더와 페이지별 텍스트 동일"로 무손상 증명. 구현: `온랩\작업\embed_sign_v13.py`(은평 donor 구조·크기 4곳 동일 L001).

## L080 | 문서작성 | 2026-07-23 | 사업계획서 글머리(ㅇ/-) 뒤 괄호 라벨은 괄호 포함 볼드 처리
- **트리거:** 사업계획서, 괄호 라벨, (문제), (한계), 글머리, 볼드, 강조, 서식
- **실수:** 사업계획서 본문에서 글머리 기호(ㅇ/-) 바로 뒤의 분류 라벨 `(문제)`·`(데이터 파편화)` 등을 일반 굵기로 뒀다가 사용자가 비포/애프터 스크린샷으로 볼드 처리를 지시하게 했다.
- **재발방지:** 사업계획서류 산출물에서 **문단 선두(글머리 ㅇ/- 직후)의 괄호 라벨은 여는 괄호부터 닫는 괄호까지 통째로 볼드** 처리한다. 문장 중간의 괄호(예: 감(感))는 대상 아님. 적용 후 렌더 눈검증(L005)과 직전 확정판 회귀 점검(L077)을 함께 돌린다. (사용자 지정 2026-07-23)

## L083 | 문서작성 | 2026-07-16 | HWPX 표 셀 채움은 데이터 셀도 검정 charPr 강제 — 템플릿 파랑 상속 주의
- **트리거:** hwpx, 채움, 파랑, 검정, charPr, set_cell, 유색, 제출본, 사업계획서 표
- **실수:** HWPX 표 채움에서 서술(안내문구) 셀엔 검정 charPr(63)을 강제했지만 **데이터 셀(조직도·BM·경쟁사·재무 등)엔 강제 안 하고 템플릿 charPr 상속** → 빈 데이터 셀의 템플릿이 파랑(#0000FF charPr9)이라 채운 내용 135런이 전부 파래짐. 검증 때 **특정 파랑 charPr ID만** 봐서 "파랑 0"으로 오판.
- **재발방지:** ①HWPX set_cell_lines 채움 시 **모든 내용 셀에 검정 charPr(함초롬바탕 11pt) 강제**. ②채움 후 반드시 hwpx_acceptance.run_hwpx_acceptance 로 colored 검증 + **런 단위로 유색 텍스트 실제 존재 확인**(특정 ID만 보지 말 것). ③유색 charPr(정규 hex, L022) 전체를 textColor=#000000 로 정규화. 제출본=검정 원칙(submission-common-postprocess).

## L084 | 문서작성 | 2026-07-15 | 지역·권역 선택은 자동 추론 금지 — 반드시 질문
- **트리거:** 컨설팅 지역, 권역 선택, 지역 체크박스
- **실수 위험:** 주소(서울) 기반이면 서울강원 자동선택 — 사용자 실제 선택은 경기인천(전략적).
- **재발방지:** 지역류 선택은 데이터로 유도 가능해 보여도 자동선택 금지, needs_confirm으로 질문. (이번엔 질문해서 맞았음 — 규칙으로 고정)

## L086 | 문서작성 | 2026-07-27 | 양식의 '빈 칸'이 사실은 체크박스 폼 컨트롤일 수 있다 — 채우기 전 칸의 정체부터 확인
- **트리거:** 양식 채움, 빈 칸, 체크, 모집분야, 학력, 선택칸, hwpx, checkBtn
- **실수:** 울산 전문상담위원 신청서에서 모집분야·학력 칸을 텍스트 추출로 보니 `''` 라 '빈 칸'으로 판단하고 `√ 경영전략` 같은 글자를 덧썼다. 실제로는 `hp:checkBtn caption="경영전략"` 폼 컨트롤이라 렌더에서 **라벨이 중복 표시**됐다(체크박스 옆에 내가 쓴 글자가 또). 학력표 첫 행(고등/학사/석사수료/석사졸업/박사)도 같은 이유로 통째 미체크로 남을 뻔했다.
- **재발방지:** 값 칸을 채우기 전 그 칸의 **자식 요소 종류를 먼저 조회**한다 — `hp:checkBtn`·`hp:comboBox`·`hp:edit` 등 폼 컨트롤이 있으면 글자를 쓰지 말고 컨트롤 속성(`value="CHECKED"`)만 바꾼다. 텍스트 추출값이 `''` 인 것은 '빈 칸'의 증거가 아니다. 문서 전체 `checkBtn` 목록을 caption 과 함께 한 번 덤프해 **체크 대상 후보를 전수 확인**하고(표 하나만 보지 말 것), 채운 뒤 렌더로 눈검증한다(L005).

## L087 | 문서작성 | 2026-07-27 | 한글 COM '찾기' 직후는 선택 상태 — 그대로 삽입하면 찾은 글자가 지워진다
- **트리거:** 한글 COM, InsertPicture, 서명 삽입, RepeatFind, 찾기
- **실수:** `RepeatFind` 로 '신청인 박다솜' 을 찾은 뒤 바로 `InsertPicture` 를 호출해, **선택돼 있던 '신청인 박다솜' 이 서명 이미지로 치환·삭제**됐다. 렌더에는 `(서명 또는 인)` 만 남아 이름이 사라졌다.
- **재발방지:** 찾기 계열 액션 뒤에는 **반드시 `hwp.HAction.Run("Cancel")` 로 선택을 푼 다음** 삽입·입력한다(`MoveRight` 는 선택 해제가 안 될 수 있음). 삽입 후에는 원래 텍스트가 살아 있는지 PDF 텍스트 추출로 대조한다 — "이미지가 들어갔다"는 성공 증거가 아니다.

## L088 | 문서작성 | 2026-07-27 | HWPX 그림 축소는 scaMatrix 로 — hp:sz 만 줄이면 잘리고, imgRect 까지 줄이면 이중축소
- **트리거:** hwpx, 그림, 서명, 크기, sz, imgRect, scaMatrix, 안 보임
- **실수:** 서명이 155mm 로 들어가 `hp:sz` 만 18mm 로 줄였더니 그림은 원본 크기로 그려지고 작은 상자에 **잘려서 투명한 좌상단만** 보였다(=서명이 안 보임). 이어서 `imgRect` 까지 같이 줄였더니 `scaMatrix` 와 겹쳐 **이중 축소**돼 점 하나만 남았다.
- **재발방지:** 그림 크기는 ①`hp:sz`·`hp:curSz` = 표시 크기 ②`hp:orgSz`·`hp:imgRect` = **원본 그대로 유지** ③실제 축소는 `hc:scaMatrix` 의 `e1`(가로)·`e5`(세로) = 표시/원본 비율. 셋을 섞어 줄이지 마라. 더 확실한 방법은 **삽입 전에 PNG 자체를 목표 크기로 리사이즈**하는 것 — 한글은 PNG 의 pHYs(dpi)를 그대로 믿지 않고 약 76dpi 로 환산하므로(실측: 260px→86mm), 목표 mm × 3.02 px/mm 로 만들어 `sizeoption=0`(원래 크기)로 넣는다. 크기를 XML 로 사후 변경하면 한글이 **줄바꿈은 원본 크기로 계산**해 줄이 밀린다.

## L089 | 문서작성 | 2026-07-27 | 공고 본문을 떼어 제출본을 만들 때 secPr 든 첫 문단은 지우지 마라 — 여백이 기본값으로 돌아가 표가 잘린다
- **트리거:** 제출본 분리, 서식만 남기기, secPr, 여백, 표 잘림, hwpx
- **실수:** 공고 전문에서 서식1·2만 남기려고 앞 문단 45개를 지우면서 `secPr`(구역설정)을 `[서식 1]` 문단으로 복사해 옮겼다. 파일 자체에는 여백 20mm 가 남았지만 **한글이 그 secPr 를 거부하고 기본 여백 30mm 를 적용**해, 모집분야 표와 제목이 오른쪽으로 잘렸다.
- **재발방지:** `secPr` 를 가진 **첫 문단은 삭제하지 말고 그 자리를 재활용**한다 — 첫 문단의 `paraPrIDRef`·`styleIDRef`·run 의 `charPrIDRef` 를 남길 첫 서식 문단의 것으로 바꾸고 텍스트만 교체한 뒤, 그 뒤 문단들을 지운다. 분리 후에는 반드시 **한글로 한 번 열었다 저장한 파일에서 `pagePr/margin` 을 다시 읽어** 원본과 같은지 실측한다(파일 안의 값 ≠ 한글이 적용하는 값).

## L090 | 문서작성 | 2026-07-27 | 한글은 문단의 pageBreak 를 되살린다 — 빈 페이지는 속성이 아니라 문단 구조로 없애라
- **트리거:** 빈 페이지, pageBreak, 쪽 나눔, 제출본 분리, 한글 COM
- **실수:** 제출본 첫 장이 `- 1 -` 만 있는 빈 페이지로 나왔다. 원인은 `[서식 1]` 문단의 `pageBreak="1"`(원래 공고 본문 뒤에 오도록 걸린 쪽나눔). XML 에서 `0` 으로 바꿔도 **한글이 파일을 열 때마다 1 로 되살려** 빈 페이지가 계속 생겼다.
- **재발방지:** 한글이 되살리는 속성은 값을 고치지 말고 **문단 구조 자체를 바꾼다** — 쪽나눔이 걸린 문단을 없애고 그 내용을 앞 문단(첫 문단)으로 옮긴다. 그리고 "고쳤다"의 확인은 XML 이 아니라 **한글로 재저장한 뒤의 PDF 페이지 수**로 한다.

## L091 | 문서작성 | 2026-07-27 | 표 폭 계산은 colSpan=1 셀만 — 병합된 제목 셀 폭을 열 폭으로 쓰면 표가 깨진다
- **트리거:** 표 폭, 여백 침범, cellSz, colSpan, 병합, 잘림
- **실수:** 경력표가 본문폭을 9.3mm 초과(원본 양식 결함)해 열 폭을 비례 축소하면서, 폭을 **행0 셀들의 합**으로 계산했다. 모집분야 표는 행0 이 4칸 병합된 제목 셀이라 그 폭(48255)을 열0 폭으로 착각해 라벨 열을 통째로 늘렸고, **표가 320mm 로 깨져** 페이지 밖으로 나갔다.
- **재발방지:** 표의 실제 폭 = **`colSpan="1"` 인 셀들의 열별 폭 합**으로 구한다(행0 만 보지 말 것). 축소할 때는 병합 셀 포함 **모든 `cellSz` 를 같은 비율로** 곱하고, 고친 뒤 다시 같은 방식으로 폭을 재측정해 본문폭 이내인지 확인한다. 폭이 바뀐 칸은 `linesegarray` 를 제거한다(해당 칸만 — L074).

## L151 | 문서작성 | 2026-08-04 | Windows Python에서 /tmp 백업 경로가 전체 작업을 조용히 중단시킴
- **번호이력:** 표준 미달 표기("## L1 — 제목", 파이프 구분자 없음)로만 기록돼 있었고 `app/tests/lessons_coverage.json` 도 이 표기를 그대로 따라 malformed 상태였다(정규식 기반 헤더 파서가 이 항목을 스킵해 전수조사에서 처음엔 누락될 뻔함). 2026-08-07 레지스트리 정리로 표준 형식 + 정식 번호(L151, 당시 최대번호 L150 다음)로 정정. L112(Bash 히어독→Python 에 `D:\` 백슬래시 소실)·L144(Bash 가 만든 `/tmp/...` 를 Python 이 못 읽음, "L112 의 반대방향")와 같은 "Windows/Python 경로 표기" 계열이지만, 이 항목은 **파일 쓰기 시점**에 `/tmp` 를 그대로 백업 경로로 써서 실패하는 별개의 구체적 사고라 병합하지 않고 별도 번호를 유지한다.
- **트리거:** Windows Python 스크립트에서 임시/백업 파일을 `/tmp/...` 로 쓸 때.
- **실수:** `shutil.copy(p, '/tmp/mailag/sites.json.bak')` 가 FileNotFoundError → sites.json 업데이트 전체가 중단됐고(옛 404 URL 그대로), 예외를 늦게 알아챔.
- **재발방지:** Windows에선 `/tmp` 대신 `os.path.join(tempfile.gettempdir(), ...)` + `os.makedirs(exist_ok=True)` 사용. 백업 실패가 본작업을 막지 않게 예외 격리.

## L092 | 작업방식 | 2026-07-28 | 이전 턴에서 본 폴더·파일이라도 인용 전 실재 재확인 — 다중 세션 환경에선 사라진다
- **트리거:** 증빙, 파일 목록, 경로 인용, 이미지 삽입 목록, 폴더, 다중 세션
- **실수:** 비앤코 서식3 그림 삽입 목록을 만들며 앞 턴 스캔에서 봤던 `합본_원본자료\10_네트워크_베트남 홈페이지.jpg` 등 파일명을 그대로 인용했는데, 그 사이 **`합본_원본자료` 폴더가 통째로 사라져**(다른 세션·정리) 목록 전체가 헛경로였다. 사용자가 그 목록으로 작업했으면 파일을 못 찾았을 것.
- **재발방지:** 산출물(목록·지시서)에 **파일 경로를 적기 직전 반드시 실재를 재확인**한다. 한글 경로는 PowerShell `Test-Path -LiteralPath` 가 NFC/NFD 정규화 차이로 **있는데도 MISSING** 을 낼 수 있으니, 파이썬으로 **상위 폴더를 `os.listdir` 로 순회해 `unicodedata.normalize('NFC')` 비교**로 매칭한다(경로 문자열 직접 조립 금지). 확인 결과는 OK/MISS 로 출력해 증거를 남긴다.

## L146 | 개발 | 2026-07-28 | 제외 필터를 제목 앵커 + 링크 도메인 두 축으로 만들면 교차 구멍이 생긴다 — 축마다 우회로를 따로 확인하라
- **번호이력:** 최초 L092 로 잘못 배정됨(같은 날 다른 세션이 이미 L092=파일경로 재확인 규칙에 씀, L118 이 경고하는 번호충돌 사례). 2026-08-07 레지스트리 정리로 L146 재번호. 원 L092 는 line 504 항목(파일경로 재확인) 참조.
- **트리거:** 오탐 차단, 블랙리스트, 제목 완전일치, 링크 도메인, recall, 필터 추가, non_notice
- **실수:** 비공고(기관소개·약관) 오발송을 막는 필터를 만들며 안전장치로 `NOTICE_SIGNAL_TOKENS`(제목에 '모집·공고·신청'이 있으면 무조건 통과)를 넣었다. 하지만 이 우회로를 **제목 블랙리스트에만** 유효하게 설계했고 **링크 도메인 룰**(youtube/instagram 차단)에는 토큰 목록이 부족해, '중소기업 홍보영상 제작 지원(유튜브)' + youtube 링크 같은 진짜 공고가 막힐 수 있었다. 적대적 반증 에이전트가 잡아냈다.
- **재발방지:** 차단 조건이 2개 이상(제목·링크·본문…)이면 **면제 토큰이 모든 축에 대해 성립하는지 축별로 실제 사례를 만들어 확인**한다. 면제 토큰 보강은 필터를 느슨하게만 만들어 recall 을 해칠 수 없으니 **의심되면 넣는 쪽**이 맞다(단, 넣은 뒤 기존 정크가 여전히 차단되는지 회귀테스트로 고정). 그리고 이런 검증은 자기 컨텍스트에서 하지 말고 **반증 전담 레인**을 따로 띄운다 — 자기검증은 통과시키는 방향으로 편향된다.

## L093 | 환경 | 2026-07-28 | git fetch 는 조용히 죽는다 — 긴 세션 시작 때 origin/main 커밋 날짜를 눈으로 확인하라
- **트리거:** git fetch 실패, bad object, refs/heads, 원격 최신 아님, 며칠 전 커밋, 동기화 안 됨
- **실수:** `.git/refs/heads/` 에 `main (1)`(공백·괄호 든 잔재 파일)이 생겨 **모든 fetch/pull 이 `fatal: bad object` 로 실패**했는데, status·commit·log 는 정상이라 **4일간(07-17~07-20) 로컬이 옛 상태에 갇힌 걸 몰랐다.** "새 커밋이 없다"와 겉보기가 같아서 놓쳤다.
- **재발방지:** 긴 세션·중요 작업 착수 전에 `git log origin/main -1 --date=short --format=%cd` 로 **원격 마지막 커밋 날짜를 눈으로** 본다(1초, 매우 값쌈). 이상하면 `Get-ChildItem .git/refs/heads -Recurse` 로 이름에 공백·괄호 든 ref 를 찾고, 지우기 전 `git merge-base --is-ancestor <해시> origin/main` 이 exit 0 인지 확인(아니면 `git branch rescue <해시>` 로 보존)한 뒤 백업·제거한다.

## L094 | 개발 | 2026-07-28 | "ahead N" 은 미병합의 증거가 아니다 — squash 병합 repo 에선 내용 해시로 확인하라
- **트리거:** 백업 필요, 미병합 브랜치, ahead N commits, 유실 위험, 브랜치 정리, squash merge, git cherry
- **실수:** `feat/listonly-detail-enrich` 가 origin/main 대비 `ahead 4` 로 보여 "백업 안 된 커밋 4개가 유실될 수 있다"고 사용자에게 경고했다. 실제로는 그 repo 가 **squash 병합**을 쓰기 때문에 커밋 SHA 만 다를 뿐 내용은 이미 main 에 들어가 있었다 — `git diff origin/main...branch` 가 **0줄**, 커밋 트리 해시도 병합본과 동일. 있지도 않은 데이터 유실 위험으로 불필요한 불안을 줬다.
- **재발방지:** 브랜치가 미병합인지 판단할 때 `ahead/behind` 숫자를 근거로 쓰지 않는다. **① `git diff origin/main...<branch> --stat` 이 비면 내용상 병합 완료** ② `git cherry origin/main <branch>` 의 `+` 표시 커밋만 진짜 미반영 ③ 애매하면 `git rev-parse <커밋>^{tree}` 로 트리 해시를 병합본과 대조. 확인 전에는 "유실 위험" 같은 강한 표현을 쓰지 말고, 확인 후 결과만 말한다.

## L095 | 문서작성 | 2026-07-30 | 양식 채우기 착수 전에 '원본 페이지 수'를 먼저 재라 — 기준선 없이 고치면 왕복이 3배
- **트리거:** hwp, hwpx, 양식, 채우기, 서명, 페이지, 밀림, 레이아웃
- **실수:** 위촉승낙서를 채운 뒤에야 4쪽인 걸 발견했는데, 원본이 2쪽인지 3쪽인지 몰라 '밀린 것인지'조차 판정할 수 없었다. 원본 PDF를 뒤늦게 뽑느라 한글 COM 왕복이 1회 늘고, 원인(서명 높이/주소 줄넘침)을 하나씩 분리하느라 총 5회 기동했다.
- **재발방지:** 채우기 **착수 즉시** 원본 양식을 PDF로 1회 렌더해 `페이지 수 + 각 페이지 마지막 문단`을 기준선으로 남긴다. 이후 산출물은 이 기준선과 페이지 수를 비교해 판정한다(같으면 통과, 다르면 원인 분리). 비용은 한글 1회, 절약은 최소 2회.

## L096 | 문서작성 | 2026-07-30 | 서명 이미지는 treatAsChar=0(떠있는 배치)로 넣어라 — 글자처럼 넣으면 줄 높이가 커져 페이지가 밀린다
- **트리거:** 서명, 도장, 이미지, 삽입, hwpx, pic, treatAsChar, 페이지 밀림
- **실수:** 서명을 `treatAsChar="1"`(글자처럼 취급) + 30.7×20.4mm 로 넣었더니 그 줄의 높이가 20mm로 커져 2쪽 문서가 4쪽이 됐다. 크기를 18×12mm 로 줄여도 3쪽이었다.
- **재발방지:** 서명·도장은 `hp:pos treatAsChar="0"` + `textWrap="IN_FRONT_OF_TEXT"` + `vertRelTo="PARA"/horzRelTo="COLUMN"` 로 넣어 **줄 높이에 영향을 주지 않게** 한다. 배치 좌표는 추측하지 말고 **채운 PDF 에서 앵커 단어(이름) 의 bbox 를 fitz 로 실측**해 `horzOffset = (단어 x1 + 여백 3mm - 좌여백)`, `vertOffset ≈ -900` 으로 계산한다. 크기는 승인 이력이 있는 값(박다솜 서명 = 5102×3391 HWPUNIT = 18×12mm)을 재사용한다.

## L097 | 문서작성 | 2026-07-30 | 한 줄짜리 칸에 긴 값을 넣으면 줄이 넘쳐 문서 전체가 밀린다 — 넣기 전에 폭을 계산하라
- **트리거:** 주소, 긴 값, 줄넘침, 한 줄, 페이지 밀림, 양식
- **실수:** 주소를 `서울 마포구 어울마당로3길 11, 301호(합정동, 동삼주택)` 로 전부 넣어 한 줄을 넘겼고, 그 1줄 때문에 문서 마지막 줄이 다음 장으로 밀렸다. 서명 크기 문제와 겹쳐 원인 분리에 왕복이 더 들었다.
- **재발방지:** 본문 인라인 칸(`라벨 : ___`)에 값을 넣기 전에 `라벨+값` 글자 수 × 글자폭 vs 본문폭(= pagePr.width − margin.left − margin.right)을 계산해 넘치는지 먼저 본다. 넘치면 ①법정동·건물명 괄호부 등 **참고정보만** 생략하거나 ②사용자에게 축약 승인을 받는다. 임의로 사실을 바꾸지 말고, 생략한 부분은 보고에 명시한다.

## L098 | 문서작성 | 2026-07-30 | 한글 COM 은 '작업당 1회'로 설계하라 — 확인하려고 다시 띄우는 순간 비용이 배로 뛴다
- **트리거:** 한글, COM, hwp, 변환, 저장, PDF, 느림, 오래 걸림
- **실수:** 위촉승낙서 하나 만드는 데 한글을 5회 기동했다(원본변환 1 + 산출물저장 2 + 원본기준선 1 + 재작업 1). 한글 기동·종료는 회당 20~40초라 이것만으로 체감 지연의 대부분을 차지했고, 사용자가 "왜 이렇게 오래 걸리냐"고 세 번 물었다.
- **재발방지:** 비싼 것은 '작업 횟수'가 아니라 **기동 횟수**다. Dispatch 한 COM 객체는 살아 있는 동안 열기·저장을 몇 번이든 할 수 있으므로, **스크립트를 쪼개지 말고 한 프로세스에서 hwp객체를 1개만 만들어 전 단계를 처리한다** — ①원본 열기→HWPX 저장→PDF(기준선) 저장 ②(같은 프로세스에서 Python 으로 XML 채움·서명) ③결과 열기→HWP 저장→PDF 저장. **목표는 기동 1회.** 이번 실패의 직접 원인은 fill/sign/export 를 별도 .py 3개로 나눠 실행해 매번 새로 띄운 것이다. 출력을 .hwpx 로 납품하고 눈검증을 생략할 수 있으면 **0회**(입력도 .hwpx 일 때). "확인차 한 번 더"가 나오면 설계가 틀린 것이다.

## L099 | 개발 | 2026-07-30 | 중간 판정에 페이지 이미지를 쓰지 마라 — 숫자로 판정하고 눈검증은 최종 1회
- **트리거:** 렌더, 이미지, PNG, 확인, 눈검증, 토큰, 페이지 수, 겹침
- **실수:** 레이아웃이 밀렸는지 확인하려고 중간 산출물 페이지를 이미지로 4장 읽었다. 이미지 1장이 수천 자 분량 토큰이라 사용자가 "왜 토큰을 이렇게 많이 쓰냐"고 지적했다. 정작 그 4장에서 얻은 정보는 "몇 쪽인가"(숫자)였다.
- **재발방지:** 판정 가능한 것은 전부 숫자로 뽑는다 — 페이지 수·문단 좌표·겹침·글자 유실은 `fitz` 의 `page_count` / `get_text("words")` bbox 로 확인(토큰 거의 0). **이미지는 최종 1장만**, 그것도 전체 페이지가 아니라 **확인할 영역만 크롭**해서 본다(`page.get_pixmap(clip=fitz.Rect(...), dpi=110)`) — 서명·값 칸 주변 50×30mm 면 전체 페이지 대비 토큰이 1/5 이하다. 눈검증 자체는 폐기하지 않는다(L001·L031) — 범위와 횟수만 줄인다. 겹침은 숫자로도 잡힌다: 인접 단어 bbox 가 교차하면 겹침이다.

## L100 | 문서작성 | 2026-07-30 | 사용자 고정 정보(인적사항·계좌)는 첫 사용 때 메모리에 박아라 — 매번 옛 서류를 뒤지는 건 낭비다
- **트리거:** 인적사항, 주소, 계좌, 주민등록번호, 성명, 소속, 신청서, 위촉서, 동의서
- **실수:** 성명·소속·주소를 채우려고 예전 신청서를 찾아 열어 추출했고, 계좌·주민번호는 어디에도 없어 결국 사용자에게 물었다. 사용자가 "다음부터 계좌는 ~" 라고 알려주기 전까지 저장 시도조차 안 했다.
- **재발방지:** 양식에 사용자 고정 정보를 채우는 순간, 그 값들을 **한 곳(auto-memory)에 표로 저장**하고 이후에는 그 표만 본다. 사용자가 값을 알려주면 즉시 저장하고 "다시 묻지 않겠다"고 확인한다. 표기 규칙(예: 주민번호 뒷자리 1자리까지)도 값과 함께 적는다. 정본 = 메모리 `parkdasom-form-identity`.

## L101 | 진단 | 2026-07-31 | 프로세스 개수를 창(세션) 개수로 읽지 마라 — 최상위만 세야 한다
- **트리거:** 세션, 창, 몇 개 열려있나, claude.exe, 프로세스 수, 작업관리자, 메모리 정리, 정리해줘
- **실수:** `claude.exe` 가 24개인 것을 보고 "클로드 창이 24개 열려 있다"고 사용자에게 단정했다. 실제로는 **최상위 창 1개 + 그 창이 띄운 서브에이전트 자식 23개**였다. 사용자는 그 말을 믿고 "안 쓰는 창 정리해줘"라고 했는데 닫을 창이 애초에 없었다. 같은 착오로 "중복 MCP 22벌은 클로드 탓"이라고도 했으나 실제 부모는 codex.exe 였다.
- **재발방지:** 창(세션) 수는 **최상위 프로세스만** 센다 — "부모가 같은 이름이 아닌 것"이 창이다. `Get-CimInstance Win32_Process` 로 부모 체인을 만들어 top-level 을 걸러낸 뒤 보고한다. 개수를 말하기 전에 **반드시 부모를 추적**하고, 어떤 앱이 무엇을 띄웠는지 확인한다(중복 프로세스를 보면 "누가 부모인가"부터). 도구: `C:\Users\ekth3\mem-guard\ai_sessions.ps1`.

## L102 | 진단 | 2026-07-31 | WorkingSet 합계로 "회수 예상 메모리"를 말하지 마라 — 공유페이지가 중복 계산된다
- **트리거:** 메모리, RAM, 회수, 확보, 정리하면 얼마, 몇 GB, WorkingSet, 프로세스 메모리
- **실수:** 유휴 node.exe 64개의 WorkingSet 합계가 2,403MB 라서 "정리하면 2.4GB 회수된다"고 사용자에게 약속했다. 실제 회수는 **0.5GB** 였다. WorkingSet 에는 여러 프로세스가 공유하는 DLL·런타임 페이지가 프로세스마다 중복으로 잡히기 때문이다. 같은 바이너리를 쓰는 프로세스가 많을수록 과대평가가 커진다.
- **재발방지:** 다수 동종 프로세스의 회수량을 예측할 땐 WorkingSet 합계를 그대로 쓰지 않는다. `PrivateMemorySize64`(전용 메모리)를 보고, 그마저도 **"실제로는 이보다 적을 수 있다"고 범위로** 말한다. 가장 정직한 방법은 **작은 표본을 먼저 종료하고 `FreePhysicalMemory` 전후 차이를 실측**한 뒤 나머지를 외삽하는 것. 예측치를 단정형으로 말하지 말 것.

## L103 | 개발 | 2026-07-31 | CIM 의 PID 는 UInt32 — 해시테이블 키로 쓸 땐 [int] 로 통일하라 (조용히 실패한다)
- **트리거:** Get-CimInstance, Win32_Process, ProcessId, ParentProcessId, 프로세스 트리, 자식 찾기, 해시테이블, PowerShell
- **실수:** 프로세스 트리를 만들려고 `$byParent[$p.ParentProcessId]` 로 해시를 채우고 `$byParent.ContainsKey([int]$cur)` 로 조회했다. CIM 이 주는 PID 는 **UInt32** 라 Int32 키와 매칭되지 않아 자식을 하나도 못 찾았다. 예외가 안 나고 **"조용히 아무 일도 안 함"** 으로 끝나서 스크립트가 정상 동작한 줄 알았다.
- **재발방지:** CIM 결과를 해시 키·값으로 쓸 때는 **넣을 때도 꺼낼 때도 `[int]` 로 캐스팅**한다. 함정 심화: `Where-Object { $_.ParentProcessId -eq $c }` 는 PowerShell `-eq` 의 타입 강제변환 덕에 **정상 동작**하므로, 인라인 테스트는 통과하는데 해시 최적화 버전만 실패하는 형태로 나타난다. 트리 탐색 스크립트는 **"찾은 자식 수가 0인가"를 먼저 확인**하고 0이면 타입 불일치를 의심하라.

## L104 | 진단 | 2026-08-04 | PowerShell `-SimpleMatch` 에 `|` 정규식을 넣지 마라 — 0건이 "미등록"으로 오판된다
- **트리거:** Select-String, SimpleMatch, 파이프 패턴, grep 대체, 등록 여부 확인, 트리거 검색, 미등록 판정
- **실수:** 스킬 전역적용 상태를 조사하며 `Select-String -Pattern "korean-code-comments|코드해석" -SimpleMatch` 를 썼다. `-SimpleMatch` 는 패턴 전체를 **리터럴 문자열**로 보므로 `"korean-code-comments|코드해석"` 이라는 문자열을 찾다가 0건이 나왔고, 실제로는 CLAUDE.md·AGENTS.md 에 **등록돼 있는데도 "어디에도 없음"** 이라고 결론 낼 뻔했다.
- **재발방지:** 대안(`|`)·와일드카드가 들어간 패턴엔 **`-SimpleMatch` 를 절대 쓰지 않는다**(정규식이면 그냥 기본 모드). 더 안전한 습관: 존재 여부 확인은 **전용 Grep 도구**로 하고, `0건`이 나오면 곧바로 결론 내지 말고 **패턴 하나만 단독으로** 다시 검색해 교차 확인한다. "없다"는 판정은 있다는 판정보다 오탐 비용이 크다.

## L105 | 개발 | 2026-08-04 | SKILL.md frontmatter 의 `description: [한글] …` 은 **깨진 YAML** 이다 — 관대한 로더가 숨겨준다
- **트리거:** SKILL.md, frontmatter, description, [한글], 스킬 패키징, .skill, yaml, Invalid YAML, 전역적용
- **실수:** `korean-code-comments` 를 전역적용하며 파일 미러·트리거만 확인하고 "정상"이라 봤다. `.skill` 패키징을 돌리자 `Invalid YAML in frontmatter` 로 실패 — `description:` 값이 `[` 로 시작하면 YAML 이 **flow sequence(배열)** 로 파싱을 시작했다가 뒤의 일반 텍스트에서 깨진다. Claude Code 의 관대한 로더는 그냥 읽어줘서 **몇 달간 아무도 몰랐다**.
- **재발방지:** frontmatter 값이 `[`·`{`·`*`·`&`·`:` 로 시작하거나 따옴표가 섞이면 **블록 스칼라(`description: >-` + 다음 줄 2칸 들여쓰기)** 로 감싼다. 스킬을 만들거나 전역적용할 때는 로딩 여부만 믿지 말고 **`yaml.safe_load` 로 frontmatter 를 1회 파싱**해 검증한다. 같은 `[한글]` 접두 패턴을 쓰는 다른 스킬(`where-to-run-guidance`, `skill-creator` 등)도 동일 결함 후보다.
- **후속(2026-08-04, 같은 날 해소):** PC 전체 전수조사 결과 동일 결함 **15개**(`where-to-run-guidance`·`github`·`test`·`test-fix`·`실제사용`·`actual-use`·`approval-15min/30min/window`·`project-health-auditor`·`omc-reference`·`skill-creator`·`skywork-design/music-maker/search`)를 블록 스칼라로 일괄 수정하고 설명문 글자 동일·본문 무손상을 대조 검증했다. 플러그인 244개·아카이브 9개·프로젝트 스킬 10개·Cursor 규칙 8개는 **깨짐 0개**로 확인. 전수조사 스크립트: `fix_skill_yaml.py`(yaml.safe_load 로 판정, --apply 시 백업+쓰기후 재검증+실패 원복).

## L106 | 도구사용 | 2026-08-04 | PowerShell 에서 `python - @'…'@` 로 스크립트를 stdin 에 넘기지 마라 — REPL 로 빠져 멈춘다
- **트리거:** PowerShell, python, 히어스트링, here-string, `python -`, stdin, 인라인 스크립트, 일회성 집계, JSON 요약, 타임아웃, 응답 없음
- **실수:** 감사 결과 JSON 을 요약하려고 `python - $json @'...(파이썬 코드)...'@` 를 실행했다. PowerShell 의 here-string 은 **인수로 전달**되지 처리기의 stdin 으로 연결되지 않아, `python -` 가 읽을 게 없어 **대화형 REPL 로 진입**했고 5분 타임아웃까지 매달렸다(출력엔 `_pyrepl` 트레이스백만 반복). 앞서 성공했던 Bash 도구의 `python - <<'PY'` 습관을 PowerShell 에 그대로 옮긴 것이 원인이다.
- **재발방지:** 한 줄이 넘는 파이썬은 **임시 `.py` 파일로 저장한 뒤 `python <파일> <인수>`** 로 실행한다(`$CLAUDE_JOB_DIR/tmp` 사용). PowerShell 에서 stdin 이 꼭 필요하면 `Get-Content x.py | python -` 처럼 파이프로 준다. 셸이 바뀌면 히어독 문법도 바뀐다 — Bash 관용구를 PowerShell 에 복사하지 않는다.

## L107 | 진단 | 2026-08-04 | 훅·설정 설치 여부는 **전역부터** 확인하라 — 폴더별만 보면 "미설치"로 오판한다
- **트리거:** 훅, hooks, settings.json, UserPromptSubmit, closeout, 설치 여부, 미설치, 폴더별 설정, 전역 설정, ~/.claude/settings.json
- **실수:** `broadcast_closeout.py` 가 각 작업폴더의 `.claude/settings.json` 만 뒤져 훅 설치를 판정했다. 결과 10곳 중 7곳이 "훅 없음"으로 나와 "그 폴더는 깃발을 꽂아도 소용없다"는 **틀린 안내**를 출력했다. 실제로는 `~/.claude/settings.json` 에 `closeout_prompt.py` 가 **전역 등록**돼 있어 모든 폴더가 대상이었다(현재 세션이 매 턴 그 주입을 받고 있었는데도 못 알아챘다).
- **재발방지:** 설정·훅 탐지는 **전역(`~/.claude/settings*.json`) → 폴더별(`<cwd>/.claude/settings*.json`)** 순으로 보고, 전역에 있으면 전 폴더 적용으로 판정한다. 판정 로직을 짜기 전에 **지금 내 세션에 그 훅이 실제로 주입되고 있는지**를 먼저 증거로 확인한다.

## L108 | 진단 | 2026-08-04 | 예약작업 추적은 한 단계로 끝내지 마라 — 이 PC 는 숨김 `.vbs` 래퍼를 거친다
- **트리거:** 작업 스케줄러, 예약작업, ScheduledTask, 되살아나는 파일, 재생성, 로그 경로, vbs, 숨김 실행, 래퍼, 검은창, find_writer
- **실수:** "이 파일을 누가 만드나"를 추적하는 `find_writer.ps1` 이 예약작업의 **명령줄과 그 명령줄이 부르는 스크립트 1단계**만 검사했다. 실제 범인은 `\repos-handoff-sync` → `~\.claude\tools\hidden\repos-handoff.vbs`(경로 없음) → `sync_repos_handoff.ps1` **line 19**(여기에 있음) 였고, 첫 실행은 "해당 없음"이라는 **거짓 무죄**를 냈다. 검은창 깜빡임을 없애려고 vbs 래퍼를 쓰는 것이 이 PC 의 표준 패턴인데 그 사실을 반영하지 않았다.
- **재발방지:** 자동실행 추적은 **스크립트 체인을 2단계까지 재귀**로 따라간다(본문에 없으면 그 본문이 부르는 `.ps1/.py/.cmd/.bat/.vbs` 로 한 칸 더). 또 자기 자신의 프로세스는 매칭에서 제외한다(안 그러면 자기가 범인으로 잡힌다). 절차 정본: `~/.claude/skills/folder-root-organizer/scripts/find_writer.ps1`.

## L109 | 개발 | 2026-08-04 | 검사 도구를 만들 땐 **이미 알려진 결함 유형**을 반드시 검사 항목에 넣어라
- **트리거:** 감사, 점검 도구, linter, skill-auditor, frontmatter, yaml.safe_load, 자체 파서, 검사 항목 누락
- **실수:** `skill-auditor` 를 만들며 frontmatter 를 **직접 만든 관대한 파서**로 읽었다. 그래서 같은 날 다른 세션이 발견한 실제 결함(L105: `description: [한글] …` 이 깨진 YAML 이라 `.skill` 패키징이 실패 — PC 전체 15개 스킬이 해당)을 **내 감사기는 한 건도 잡지 못한다**. "우리 환경에서 실제로 터진 적 있는 결함"을 검사 목록에 넣지 않은 것이 원인이다.
- **재발방지:** 검사 도구를 설계할 때 `lessons.md` 와 위키에서 **그 도메인에서 실제로 터진 사고를 먼저 조회**하고, 각각을 검사 항목으로 매핑한다. 파싱은 관대한 자체 파서 대신 **실제 소비자가 쓰는 엄격한 파서**(여기서는 `yaml.safe_load`)로 한 번 더 검증한다. → 후속 조치 필요: `audit_skills.py` 에 YAML 유효성 검사 추가.

## L110 | 도구사용 | 2026-08-04 | 소스에 코드를 끼워 넣을 위치를 `)` 로 찾지 마라 — 함수 호출의 닫는 괄호와 구분되지 않는다
- **트리거:** 스크립트로 코드 삽입, import 블록 끝 찾기, 상수 추가, 일괄 패치, `max(i for i, ln in ...)`
- **실수:** 테스트 2개 파일에 `REPO_ROOT` 상수를 넣으려고 "마지막 import 줄"을 `startswith(('import ','from ')) or strip()==')'` 로 찾았다. `)` 는 **여러 줄 함수 호출의 닫는 괄호**와도 일치해서, 상수가 함수 **본문 중간**(`NotebookLMBrowser(...)` 호출 직후)에 삽입돼 두 파일이 모두 SyntaxError 로 깨졌다.
- **재발방지:** 삽입 지점은 **고유한 앵커 문자열**(그 파일에 한 번만 나오는 줄)을 기준으로 잡거나 `ast` 로 마지막 import 노드의 `end_lineno` 를 쓴다. 정규식·휴리스틱으로 위치를 추정했으면 **삽입 직후 `py_compile` 로 즉시 확인**한다(테스트 실행 전에 깨진 것을 안다).

## L148 | 개발 | 2026-08-04 | 격리가 먹는지 확인하기 전에 실데이터에 테스트를 쏘지 마라
- **번호이력:** 최초 L106 으로 잘못 배정됨(L106 은 이미 같은 날 도구사용 카테고리의 'PowerShell python stdin' 규칙이 선점, L118 이 경고하는 번호충돌 사례). 2026-08-07 레지스트리 정리로 L148 재번호. 원 L106 은 line 580 항목(PowerShell stdin) 참조.
- **트리거:** 스모크테스트, 훅 테스트, OMC_STATE_DIR, 환경변수 격리, 테스트 데이터, jsonl, 오염, 샌드박스
- **실수:** autoeval 훅 어댑터를 검증하며 `$env:OMC_STATE_DIR` 로 데이터 경로가 임시폴더로 바뀔 거라 **가정하고** 실행했다. 그 환경변수는 다른 용도라 안 먹혔고, 가짜 채점 기록 6+2건이 **실제 `signals.jsonl`·`evaluations.jsonl` 에 섞였다**(성적표·실패율 왜곡). 다행히 세션ID 접두어로 식별해 8건 전량 제거하고 2388/64 로 원복했다.
- **재발방지:** 부수효과(append/write)가 있는 스크립트를 테스트할 땐 ① **격리가 실제로 먹는지 1줄로 먼저 확인**한다(임시폴더에 파일이 생겼는지 / 실파일 줄 수가 그대로인지) ② 안 먹으면 테스트 데이터에 **식별 가능한 표식**(`smoketest-*` 같은 세션ID)을 반드시 넣어 나중에 정확히 걷어낼 수 있게 한다 ③ 테스트 전후 **줄 수를 찍어 오염 여부를 자동 판정**한다. "환경변수가 있으니 되겠지"는 검증이 아니다.

## L111 | 도구사용 | 2026-08-04 | 살아있는 설정을 잠시 치웠다 되돌리는 대조군 테스트 금지 — 체인이 끊기면 기능이 꺼진 채로 남는다
- **트리거:** 대조군, A/B 테스트, 인과 증명, 설정 잠시 끄고, mv config config.off, 원복, `&&` 체인, 검증
- **실수:** `wiki.autoCapture=false` 가 **원인임을 증명**하려고 `mv 설정 설정.off && 실행 && mv 설정.off 설정` 체인을 짰다. 명령이 중간(exit 107)에 끊겨 **설정이 치워진 채로 남았을 수 있는 상태**가 됐는데, 그대로 턴을 끝내고 사용자에게 상태 불명을 보고하지 않았다. 게다가 그 대조군은 애초에 불필요했다 — 며칠간 신규 파일이 0개라는 **실사용 증거**가 훨씬 강한 근거였다.
- **재발방지:** ① 설정·데이터의 **실물을 옮기는 방식으로 대조군을 만들지 않는다** — 임시 폴더에 사본을 만들어 그쪽 경로로 돌리거나, 코드의 config 로더에 값을 직접 주입한다. ② 부득이 원복이 필요하면 `&&` 체인 대신 `trap ... EXIT`(또는 try/finally)로 **실패해도 반드시 복원**되게 한다. ③ 명령이 실패로 끝난 턴은 **"지금 상태가 무엇인지"를 반드시 확인·보고**하고 끝낸다(조용히 종료 금지). ④ 인과 증명 전에 **이미 가진 실사용 증거로 충분한지** 먼저 따진다 — 위험한 실험을 안 하는 게 최선의 검증이다.

## L114 | 개발 | 2026-08-04 | 클라우드 상품코드·요금을 기억이나 추측으로 말하지 마라 — 목록/요금 API로 뽑아라
- **트리거:** 상품코드, productCode, 스펙, 요금, 가격, 얼마, 월 얼마, Micro, 무료, NCP, 인스턴스 생성
- **실수:** ①NCP 무료 Micro 코드를 `SVR.VSVR.MICRO.C001.M001...` 로 추측해 넣었다가 `3010026 상품코드 없음`으로 반려됐다. 실제로는 **그 계정에 Micro 상품 자체가 존재하지 않았다**(VPC 최소 2vCPU/4GB, Classic 상품 0개). ②요금을 기억으로 "시간당 250원·월 18만원"이라 말했으나 실제는 **123원/시간·월 88,000원**이었다. 사용자가 이 잘못된 가격으로 판단할 뻔했다.
- **재발방지:** 상품·요금은 반드시 **API로 실측**한 뒤 말한다. NCP: 상품=`GET /vserver/v2/getServerProductList?regionCode=KR&serverImageProductCode=<이미지코드>&responseFormatType=json`(**serverImageProductCode 없으면 빈 목록이 조용히 옴**), 요금=`GET https://billingapi.apigw.ntruss.com/billing/v1/product/getProductPriceList?productItemKindCode=VSVR&regionCode=KR`. MCP에 조회 도구가 없으면 서명 로직을 재사용한 임시 스크립트로 직접 호출한다. "없는 걸 있다고 가정"이 가장 비싼 오류다 — 목록에 없으면 없는 것이다.

## L115 | 개발 | 2026-08-04 | [⚠️ L117 로 정정됨] 사용자가 계속 써야 하는 서버 — 분리 프로세스(Start-Process)도 죽는다, .cmd 런처로 가라
- **트리거:** 서버 실행, uvicorn, 앱 띄워, localhost, 포트, run_in_background, 계속 켜둬, 데몬
- **실수:** FastAPI 앱을 `run_in_background` Bash 태스크로 띄우고 "이제 브라우저에서 쓰세요"라고 보고했다. 그 태스크가 종료되면서 **서버도 같이 죽었고**, 사용자는 접속이 안 되는 상태를 받았다. 검증(HTTP 200) 당시엔 살아 있었기 때문에 보고 자체는 사실이었으나 **수명을 확인하지 않았다**.
- **재발방지:** 사용자가 이어서 써야 하는 장기 프로세스는 세션과 **분리**해 띄운다 — Windows: `Start-Process -FilePath <python> -ArgumentList ... -WorkingDirectory ... -WindowStyle Hidden -PassThru` 후 **PID를 파일로 저장**(`.server.pid`)하고 끄는 명령까지 안내한다. 검증은 "지금 200이 뜬다"가 아니라 **"내 턴이 끝나도 살아 있는 방식으로 띄웠는가"**까지 확인한다.

## L116 | 공통 | 2026-08-04 | 작업 착수 전 상위 폴더 README를 먼저 읽어라 — "무엇에 쓸 건지"를 모른 채 만들지 마라
- **트리거:** 서버 만들어, 인프라, 프로비저닝, 새로 만들어, 셋업, 뭐에 쓰는, 목적, 왜 필요
- **실수:** "네이버 서버 만들어줘" 한 마디로 OS·사양·공인IP를 정하고 생성까지 시도했다. 세션 후반에 사용자가 "이게 뭐에 필요한 서버였지?"라고 묻고서야 상위 폴더 `README.md`를 읽었고, **실습 워크샵용이며 그 실습은 로컬 PC에서 무료로 가능**하다는 사실(README FAQ에 명시)이 드러났다. 그 전까지 카드 등록·요금·상품코드로 여러 턴을 소모했다.
- **재발방지:** 리소스를 새로 만들거나 돈이 드는 작업을 하기 전에 **repo 루트/상위 폴더의 README·문서를 먼저 읽어 목적과 대안을 확인**한다(§2 작업 전 확인 기준의 실사용판). 특히 사용자가 목적을 말하지 않은 채 "만들어줘"라고만 하면, 만들기 전에 **문서에서 목적을 찾아본다**. 대개 문서에 "이거 안 써도 된다"가 적혀 있다.

## L112 | 도구사용 | 2026-08-04 | Bash 히어독 안 Python 에 Windows 경로를 백슬래시로 쓰지 마라 — 조용히 글자가 사라진다
- **트리거:** heredoc, `python - <<`, Windows 경로, \\, RESUME.md 갱신, 파일 삽입, 경로 깨짐, 이스케이프
- **실수:** `RESUME.md` 에 인계 블록을 넣으며 히어독 안 Python 문자열에 `D:\\auto_write` 라고 썼다. 백슬래시가 한 겹 줄어 Python 소스에 `\a` 로 들어갔고 **BEL(0x07) 제어문자**가 돼 파일에 `D:uto_write` 로 저장됐다(글자 소실). 경고는 `SyntaxWarning: invalid escape sequence` 한 줄뿐이라 넘길 뻔했고, 다음 세션이 읽을 **인계 파일의 경로가 틀린 채 남을** 뻔했다.
- **재발방지:** ① 히어독·스크립트로 파일에 쓰는 **Windows 경로는 슬래시(`D:/auto_write/...`)로 통일**한다 — Windows 도구 대부분이 슬래시를 받는다. ② 굳이 백슬래시가 필요하면 `r'''...'''`(raw) 를 쓰거나 `chr(92)` 로 조립한다. ③ **`SyntaxWarning: invalid escape sequence` 는 경고가 아니라 오류로 취급**한다 — 그 줄에 이미 글자가 사라졌다는 신호다. ④ 파일을 쓴 뒤 `t.count(chr(7))` 등으로 **제어문자 0개를 확인**하고, 삽입한 경로를 눈으로 다시 읽는다.

## L113 | 작업방식 | 2026-08-04 | 라이브로 돌아가는 스크립트를 '리팩터 중간 상태'로 두고 세션을 넘기지 마라
- **번호이력:** 이 규칙은 처음부터 L113 으로 기록됐으나 표기 형식이 표준(`## L### | 카테고리 | 날짜 | 제목`)과 달라(파이프 구분자 없음) `lessons_coverage.json` 에는 들어갔어도 본문 헤더가 정본 형식이 아니었다. 2026-08-07 레지스트리 정리로 표준 형식으로 정정(번호·내용은 그대로).
- **트리거:** 실행 중인 GUI/데몬 스크립트(cockpit_widget.ps1 등)를 여러 번에 나눠 편집하는 도중 세션이 압축·중단될 때.
- **실수:** CheckBox의 Tag를 문자열 id → 객체로 바꾼 뒤 Save-Check $s.Tag 를 .id 로 못 고친 채 중단. 그 상태로 위젯이 재시작되어 체크 기록이 @{id=...; disp=...} 문자열로 저장 → 구글 할일 완료 처리가 매칭되지 않아 "체크해도 안 사라짐". 게다가 위젯 프로세스가 창 없이 떠 있는 상태가 되어 신규 할 일 등록이 며칠간 0건.
- **재발방지:** ① 한 데이터 구조를 바꾸면 그 필드를 읽는 **모든 호출부를 같은 편집 묶음 안에서** 고친다(grep으로 사용처 전수 확인 후 편집 시작). ② 라이브 스크립트는 편집 후 반드시 **재시작 + 창 실측(EnumWindows by PID)** 까지 한 턴 안에서 끝낸다. ③ 창 확인은 FindWindow(제목) 대신 **PID 기준 EnumWindows** 로 한다 — 한글 제목은 셸 인코딩에 따라 매칭이 실패해 '창 없음'으로 오판한다(이번에 실제로 오판함).

## L117 | 개발 | 2026-08-04 | "계속 켜져 있습니다"라고 단정하지 마라 — Claude 가 띄운 프로세스는 Start-Process 로도 못 살린다 (L115 정정)
- **트리거:** 서버 실행, 계속 켜둬, 창 닫아도, Start-Process, 백그라운드, 앱 띄워, 아까는 됐는데 안 돼
- **실수:** uvicorn 을 `run_in_background` 로 띄웠다가 죽자 `Start-Process -PassThru` 로 다시 띄우고 **"이제 이 대화창과 무관하게 계속 떠 있습니다"라고 단정**했다. HTTP 200 까지 확인하고 보고했지만 **그것도 세션 종료와 함께 죽었다**(PID 사망, 포트 리스닝 0). 같은 거짓 보고를 **두 번** 했고, L115 에 적은 처방("Start-Process 로 띄워라") 자체가 틀렸다.
- **재발방지:** Claude 세션이 띄운 프로세스는 **자손으로 일괄 정리**되어 어떤 방식으로도 세션보다 오래 못 산다(`schtasks` 는 권한 분류기 차단 가능). 계속 써야 하는 서버는 **처음부터 `start-*.cmd`/`stop-*.cmd` 런처를 만들어 "더블클릭하세요"로 넘긴다**. 그리고 **`HTTP 200` 은 "지금 살아있다"일 뿐 수명 보장이 아니다** — 수명을 검증 못 했으면 "지금은 떠 있고, 제 세션이 끝나면 꺼집니다"라고 정확히 말한다. 정본 스킬: `local-app-server-outlive-session`.

## L118 | 도구사용 | 2026-08-04 | lessons.md 번호는 "존재 여부"가 아니라 **최대 번호+1** 로 붙여라 — 동시 세션이 같은 번호를 쓴다
- **트리거:** lessons.md, L규칙, 교훈 기록, 번호, 중복, 여러 세션
- **실수:** 교훈을 추가하며 `"L107" not in text` 로 중복을 판정했는데, 다른 세션이 이미 L104~L113 을 써둔 상태라 **내 L104·L105·L106 이 남의 것과 번호 충돌**했고, 내 L107 은 "이미 있음"으로 오판돼 **아예 저장되지 않았다**.
- **재발방지:** 추가 전에 `grep -n "^## L[0-9]" lessons.md` 로 **실제 최대 번호를 읽고 +1** 부터 붙인다. 중복 판정은 번호 문자열이 아니라 **제목 내용**으로 한다. 여러 세션이 같은 파일에 붙이는 append-only 파일은 전부 이 규칙을 따른다.

## L119 | 도구사용 | 2026-08-04 | 위키 본문에 링크 문법을 '예시'로 적으면 그게 진짜 링크가 된다 — 백틱도 못 막는다
- **트리거:** 위키, wiki_ingest, broken-ref, 링크 문법 설명, 이중대괄호, `[[`, 문서에 예시 적기, lint 재확인
- **실수:** `wiki_lint` 의 broken-ref 27건을 0으로 정리한 직후, 그 조치를 설명하는 위키 글에 이중대괄호 링크를 **설명용 예시**로 적었다. `wiki_ingest` 가 그것을 실제 참조로 파싱해 frontmatter `links:` 에 자동 등록했고(`page-00002026.md`·`resume-folder-location.md`), **0으로 만든 직후 스스로 새 broken-ref 4건을 만들었다**. 백틱(인라인 코드)으로 감싸도 파싱을 막지 못했다.
- **재발방지:** 위키 본문에서 링크 문법을 설명할 때는 **대괄호를 쓰지 말고 말로 적는다**("이중대괄호 링크"). 마크업 문법을 예시로 인용해야 하면 그 도구가 그것을 파싱하는지 먼저 확인한다. 그리고 **문서를 고친 뒤 반드시 검사기를 한 번 더 돌려** '내가 새로 만든 문제'가 없는지 본다 — 정리 작업의 마지막 단계는 언제나 재검증이다.

## L147 | 작업방식 | 2026-08-04 | 위키 "페이지 수"를 지식량으로 착각하지 마라 — 248개 중 220개가 빈 껍데기였다
- **번호이력:** 최초 L095 로 잘못 배정됨(L095 는 이미 2026-07-30 문서작성 카테고리의 '양식 페이지수 기준선' 규칙이 선점, L118 이 경고하는 번호충돌 사례). 2026-08-07 레지스트리 정리로 L147 재번호. 원 L095 는 line 524 항목(양식 채우기 페이지수 기준선) 참조.
- **트리거:** 위키, wiki_list, wiki_lint, 페이지 수, .omc/wiki, session-log, 자동캡처, 지식베이스, 검색해도 안 나옴
- **실수:** 세션마다 헤더에 뜨는 "LLM Wiki: 248 pages" 를 보고 이 프로젝트 위키에 지식이 많이 쌓였다고 전제해 왔다. 실제로 열어 세어 보니 **220개가 OMC 자동캡처가 만든 빈 껍데기**(제목+세션ID+"Auto-captured session metadata" 한 줄)였고 내용 있는 페이지는 **28개**뿐이었다. lint 결과 398건 중 대부분도 이 껍데기가 만든 노이즈라 진짜 문제(깨진 링크 2건)가 파묻혀 있었다.
- **재발방지:** 위키를 근거로 삼기 전 **내용 있는 페이지 수를 실측**한다 — 본문에서 frontmatter·제목을 걷어낸 길이가 임계 미만이고 "Auto-captured session metadata" 를 포함하면 껍데기로 센다. 정리는 삭제가 아니라 `_archive_shells_<날짜>/` 로 **이동**(복구 가능). 깨진 링크는 본문뿐 아니라 **frontmatter 의 `links:` 배열**에도 있으니 양쪽을 고쳐야 lint 가 떨어진다. 절차는 `wiki-session-capture` 스킬의 "정기 검진·껍데기 정리" 절 참조.

## L149 | 진단 | 2026-08-04 | 같은 폴더에 세션이 여럿이면 "결과가 안 바뀜 = 캐시"로 단정하지 마라
- **번호이력:** 최초 L108 로 잘못 배정됨(L108 은 이미 같은 날 진단 카테고리의 '예약작업 추적' 규칙이 선점, L118 이 경고하는 번호충돌 사례). 2026-08-07 레지스트리 정리로 L149 재번호. 원 L108 은 line 590 항목(예약작업 추적) 참조.
- **트리거:** 동시 세션, 멀티세션, 세션 가드, wiki_lint, 결과 동일, 캐시, 반영 안 됨, 다른 세션이 수정
- **실수:** 위키 링크 7건을 고친 뒤 `wiki_lint` 를 다시 돌렸는데 **85건/27 broken 이 한 글자도 안 바뀌어** "도구 캐시"라고 사용자에게 단정해 보고했다. 실제로는 캐시가 아니라 **다른 세션 4개가 같은 위키를 동시에 손보는 중**이어서 그 시점 상태가 그랬던 것이고, 잠시 뒤 다시 돌리니 **broken 27→0**으로 정상 반영됐다. 게다가 그 세션이 나보다 철저해서(본문 `[[…]]` 뿐 아니라 frontmatter `links:` 까지 검사) 내가 "진짜 끊김 0"이라 한 것도 실은 frontmatter 오타 1건이 남아 있었다.
- **재발방지:** 세션 가드가 "다른 세션 활동 중"을 알리는 상황에서 도구 출력이 예상과 다르면 **캐시를 결론으로 삼지 말고** ① 잠시 뒤 재실행 ② 파일 실물·수정시각 확인 ③ 상대 세션이 남긴 산출물(스킬·위키·RESUME)에 같은 작업 기록이 있는지 확인 — 셋을 거친 뒤에 원인을 말한다. 또한 **착수 전에 그 주제의 기존 스킬을 먼저 읽어라**(`omc-wiki-stub-cleanup` 부록에 이미 절차·스크립트가 있었다). 안 읽어서 같은 조사를 손으로 다시 했고 검사 범위도 좁았다.

## L120 | 진단 | 2026-08-04 | 검사 도구가 "고장 N건"이라 하면 고치기 전에 **도구의 오탐부터** 의심하라

- **트리거:** 감사, 점검, 검사 결과, 오류 N건, 깨진 스킬, lint, audit, 전수검사, "고쳐줘"(검사 결과 기반)
- **실수:** skill-auditor 가 "스킬 64개 중 36건 고장"이라 보고했고, 나는 그 목록을 그대로 "수리 대상"으로 받아들여 사용자에게도 그렇게 전했다. 실제로 고장난 스킬은 **0건**이었다. 36건 전부 감사기 자신의 오탐이었다 — ①`scripts/x.py` 를 스킬 폴더 기준으로만 찾음(실제로는 `cd D:\mail` 기준) ②절대경로 정규식이 백슬래시만 인정해 `D:/…` 를 통째로 못 봄 ③cp949 콘솔에서 em-dash 출력 순간 크래시. 검사기가 틀리면 "고장 목록"이 통째로 거짓이 된다.
- **재발방지:** 검사 결과를 보고·수리하기 전에 **오류 항목 1~2건을 손으로 직접 반증**한다 — 그 파일명을 파일시스템 전체에서 검색해 "정말 어디에도 없는지" 확인하고, 있으면 **검사기의 경로 해석**을 먼저 고친다. 도구를 고친 뒤에는 **일부러 깨진 샘플**(없는 파일을 가리키는 임시 스킬)을 넣어 여전히 잡는지 확인한다 — 오탐을 줄이다 검사 자체를 무력화하지 않았음을 증명해야 한다.

## L121 | 진단 | 2026-08-04 | 로컬에 파일이 없다고 "새로 만들자"로 가지 마라 — 다른 세션이 pull 중이면 원격엔 이미 있다

- **트리거:** 파일 없음, 스크립트가 없는데, 새로 만들어야, 껍데기 스킬, 미구현, 참조 깨짐, 여러 세션, 동시 작업
- **실수:** daangn-price-check 스킬이 부르는 `daangn_collect.py`·`build_report.py` 등이 D 드라이브 전체 검색에서 **0건**이라 "진짜 고장 6건 = 스크립트를 새로 짜야 함"으로 판정하고 작성을 시작했다. 실제로는 그 스크립트들이 이미 `origin/main` 에 있었고, **다른 클로드 세션이 몇 분 뒤 auto_shopper 를 pull** 하면서 로컬에 나타났다. 이미 있는 파일 3개를 새로 짤 뻔했다(워크트리까지 만들었다가 폐기).
- **재발방지:** "없다 → 만들자" 사이에 **원격 확인**을 반드시 끼운다 — `git -C <repo> fetch --prune origin` 후 `git ls-tree --name-only origin/main <경로>`. 로컬 워킹트리는 동시 세션 때문에 언제든 낡아 있다. 파일 존재 판정에 시간이 걸리는 작업(전체 검색 등)을 했다면, **결론을 내기 직전에 한 번 더** 확인한다.

## L122 | 진단 | 2026-08-04 | 사용자가 붙여넣은 가격·수치를 검증 없이 전제로 삼지 마라 — 원본 페이지가 정본이다
- **트리거:** 최저가, 가격, 재고, 붙여넣기, 스펙, 사용자 제공 수치, 상품정보, 원문 인용
- **실수:** 사용자가 붙여넣은 TUMI 상품글의 "650,000원"을 그대로 기준가로 잡고 비교를 시작했다. 공식몰을 직접 열어보니 실제가는 530,000원이었다 — 붙여넣은 텍스트는 오래됐거나 다른 판매처 값이었다. 기준가가 틀리니 "얼마나 싼가" 판단 전체가 흔들렸다.
- **재발방지:** 가격·재고·스펙처럼 **바뀌는 수치**는 사용자가 줬더라도 **원본(공식몰·상세페이지)을 먼저 열어 확인**한 뒤 기준으로 삼는다. 값이 다르면 조용히 갈아끼우지 말고 "붙여주신 값 X, 실제 확인값 Y"로 차이를 명시한다.

## L123 | 진단 | 2026-08-04 | 스크립트·스케줄러가 뭘 하는지 추측해서 경고하지 마라 — 열어서 읽고 판단한다
- **트리거:** 작업스케줄러, 백업 스크립트, restore, .ps1, 자동 복원, 되돌아감, 덮어쓴다, 스케줄 작업
- **실수:** `claude-cred-backup` 스케줄 작업이 "2시간 뒤 새 로그인을 되돌릴 것"이라고 사용자에게 경고했다. 실제로 `claude-cred-restore.ps1` 을 읽어보니 **현재 자격증명이 정상이면 아무것도 덮어쓰지 않고 return** 하는 조건부 수동 도구였다. 불필요한 "스케줄러 끄세요" 안내로 사용자를 헛돌게 만들 뻔했다.
- **재발방지:** 남이(또는 과거의 내가) 만든 스크립트의 **부작용을 말하기 전에 그 파일을 먼저 Read** 한다. 이름(`backup`/`restore`)만 보고 동작을 단정하지 않는다. 이미 잘못 경고했으면 정정을 **명시적으로** 말한다("끄지 않아도 됩니다").

## L124 | 도구사용 | 2026-08-04 | 사용자가 직접 실행할 명령은 처음부터 `!` 접두사를 붙여서 줘라
- **트리거:** claude auth login, 사용자가 직접 실행, 로그인, 명령 실행 안내, 터미널에서 실행, ! 접두사
- **실수:** "`claude auth login` 을 실행하세요"라고만 안내했더니, 사용자가 그 문자열을 **채팅 입력창에 4번 그대로 입력**했다(= 나에게 말을 건 것). 명령은 한 번도 실행되지 않았고 "또 안 되잖아"가 반복됐다.
- **재발방지:** 사용자가 직접 실행해야 하는 명령은 **`! claude auth login`** 처럼 `!` 접두사를 포함한 형태로 제시하고 "이 줄을 그대로 붙여넣으세요(맨 앞 `!` 포함)"를 덧붙인다. PowerShell 변수(`$env:...`)는 `!` 가 bash 로 실행되므로 **절대경로로 바꿔서** 준다.

## L125 | 진단 | 2026-08-04 | 동시세션 환경에서 "내 조치 전/후 비교"는 인과가 아니다 — 지표별로 누가 바꿨는지 갈라라
- **트리거:** 전후 비교, before/after, lint 398→28, 내 조치 효과, 동시 세션, 멀티세션, 지표 개선, 원인 귀속
- **실수:** `.omc/wiki` 의 아카이브 폴더를 밖으로 옮긴 뒤 `wiki_lint` 를 다시 돌려 "이슈 398→28, **broken-ref 2→0**" 이 모두 내 이동 덕분이라고 스킬에 적었다. 실제로는 broken-ref 는 **같은 시간 다른 세션이 frontmatter `links:` 를 정리**해서 0이 된 것이었다(내 이동은 orphan·stale 만 줄인다). 파일을 직접 확인하고서야 알았다.
- **재발방지:** 여러 세션이 같은 repo를 만지는 환경에서는 전/후 스냅샷 차이를 통째로 내 공으로 돌리지 말고, **지표별로 "내 조치가 그 지표를 바꿀 수 있는 메커니즘인가"를 따진다.** 메커니즘이 설명되지 않는 개선은 다른 세션의 변경을 먼저 의심하고 **실제 파일을 열어 확인**한 뒤 기록한다. 이미 잘못 적었으면 스킬·위키의 그 문장을 즉시 정정한다(틀린 검증근거는 다음 사람을 오도한다).

## L150 | 협업 | 2026-08-04 | 여러 세션 동시 작업 중엔 "정리·삭제 착수 직전"에 대상 상태를 다시 확인하라
- **번호이력:** 최초 L125 로 잘못 배정됨(L125 는 이미 같은 날 진단 카테고리의 '전후비교 인과오판' 규칙이 선점, L118 이 경고하는 번호충돌 사례). 2026-08-07 레지스트리 정리로 L150 재번호. 원 L125 는 line 693 항목(전후비교 인과) 참조.
- **트리거:** 위키 정리, 파일 정리, 껍데기 삭제, 일괄 이동, 멀티세션, 동시 세션, 다른 창, lint 후 정리, 스냅샷, 대상 목록
- **실수:** `wiki_lint` 로 "328쪽 중 293쪽이 빈 껍데기"를 확인하고 사용자 승인까지 받은 뒤 이동을 실행했는데, 그 사이(약 4분) **다른 세션이 같은 정리를 이미 끝내** 이동 대상이 0개였다. 검증 없이 보고했으면 "293개 정리 완료"라는 **거짓 보고**가 될 뻔했다(실제 정리자는 다른 세션, 내 산출물은 빈 폴더뿐).
- **재발방지:** ①분석 스냅샷과 실행 사이에 사용자 확인·질문이 끼면 **실행 직전에 대상 개수를 재측정**하고, 0이면 "이미 처리됨"으로 경로를 바꾼다. ②일괄 처리 결과는 반드시 **처리 건수를 출력·검증**하고 그 숫자로만 보고한다(의도 ≠ 결과). ③0건이면 `.omc/wiki/log.md` 같은 **작업 로그로 누가 언제 했는지 확인**한 뒤, 남은 미완 단계(여기선 autoCapture 끄기)만 이어서 처리한다.

## L126 | 작업방식 | 2026-08-04 | 착수 전 기존 스킬을 안 읽어, 이미 적혀 있던 답을 시행착오로 다시 배웠다
- **트리거:** 위키, wiki_lint, 정리, 청소, 스킬 있나, 착수 전 점검, 중복 확인
- **실수:** 위키 껍데기 정리를 시작하면서 `~/.claude/skills/omc-learned/` 를 먼저 확인하지 않았다. 실제로는 `omc-wiki-stub-cleanup.md` 가 이미 있었고, 내가 시행착오로 알아낸 "`wiki_lint` 의 broken-ref 는 본문 `[[링크]]`가 아니라 frontmatter `links:` 배열만 본다"가 **부록 B 에 이미 적혀 있었다**. 본문만 고치고 lint 를 다시 돌렸다가 8건 그대로인 것을 보고서야 원인을 찾았다 - 스킬을 먼저 읽었으면 없었을 한 사이클.
- **재발방지:** CLAUDE.md 프로토콜 10 ①(착수 전 중복 확인)을 **작업 시작 첫 도구 호출로** 실행한다. 위키·세션·git·문서 같은 반복 주제는 거의 항상 omc-learned 에 선례가 있다: `ls ~/.claude/skills/omc-learned/ | grep -i <주제>` 를 먼저 돌리고, 있으면 신규 작성이 아니라 **읽고 → 따르고 → 새로 알아낸 것만 부록으로 갱신**한다.

## L127 | 도구사용 | 2026-08-04 | wiki_ingest·wiki_lint 의 `workingDirectory` 는 **무시된다** — 항상 세션 cwd 위키에 쓴다
- **트리거:** wiki_ingest, wiki_lint, workingDirectory, 위키 저장, .omc/wiki, 다른 폴더 위키, 프로젝트 위키
- **실수:** 쇼핑 지식을 `workingDirectory: D:\auto_shopper` 로 저장했다고 보고했는데, 실제 파일은 **세션 cwd 인 `C:\Users\ekth3\.omc\wiki`** 에 생겼다. lint 도 같은 파라미터를 줘도 홈 위키(39페이지)만 리포트했다 — auto_shopper 위키(8페이지)는 건드려지지도 않았다. 즉 **파라미터가 조용히 무시**된다.
- **재발방지:** 다른 폴더 위키에 저장해야 하면 ① 그 폴더에서 세션을 열거나(`/cwd`) ② 저장 후 **파일이 실제로 어디 생겼는지 `ls` 로 확인**하고 아니면 `mv` + 양쪽 `index.md` 손질(페이지수·목록 줄)까지 한다. "파라미터를 줬으니 됐겠지"로 보고하지 마라.


## L128 | 도구사용 | 2026-08-04 | 미커밋 변경이 있는 파일에 `git checkout <ref> -- <path>` 를 쓰지 마라 — 경고 없이 덮어쓴다
- **트리거:** git checkout -- 경로, 브랜치 갈아타며 파일만 가져오기, 미커밋 변경, 작업 옮기기, 새 브랜치에서 이어 작업
- **실수:** 방금 수정해 테스트를 통과시킨 `lessons_coverage.json`(미커밋)을, 새 브랜치로 옮기려고 `git checkout <다른브랜치> -- app/tests/lessons_coverage.json` 했다. 그 명령은 **커밋된 버전**을 가져와 내 작업본을 조용히 덮어썼다(등록 135건 → 113건으로 되돌아감). 경고도 확인도 없었고, `git status` 가 비어 있어 처음엔 "옮겨졌다"고 착각했다.
- **재발방지:** 미커밋 작업을 다른 브랜치로 옮길 땐 **먼저 커밋하거나**(가장 안전) `git diff > patch` 로 뽑아 새 브랜치에서 `git apply` 한다. `git checkout <ref> -- <path>` 는 **작업본을 버리고 되돌릴 때만** 쓴다. 브랜치를 갈아탄 직후에는 `git status` 가 비어 있는 것이 "성공"이 아니라 **"내 변경이 사라졌다"는 신호일 수 있다** — 옮겼다고 믿기 전에 파일 내용(건수·해시)을 직접 확인한다.

## L129 | 도구사용 | 2026-08-04 | Bash 도구에서 PowerShell 히어스트링 `@'...'@` 을 쓰지 마라 — 커밋 메시지에 `@` 가 박힌다
- **트리거:** git commit -m, 여러 줄 메시지, 히어스트링, @', PowerShell, Bash 도구, 커밋 메시지
- **실수:** Bash 도구에서 `git commit -m @'...'@` (PowerShell 문법)을 썼다. bash 는 `@` 를 그냥 문자로 읽어 커밋 메시지 **첫 줄과 마지막 줄에 `@` 만 남은 줄**이 생겼다. 이미 push 한 뒤라 force-push 금지 규칙 때문에 되돌릴 수도 없었다.
- **재발방지:** Bash 도구에서는 **bash 히어독**(`git commit -F - << 'EOF'` 또는 `-m "$(cat << 'EOF' ... EOF\n)"`)만 쓴다. PowerShell 히어스트링은 **PowerShell 도구에서만**. 도구 설명에 이미 금지로 적혀 있었다 — 도구를 바꿔 쓸 때 문법도 같이 바꾸는지 확인할 것. 그리고 **push 전에 `git log -1 --format=%B` 로 메시지를 눈으로 확인**하면 되돌릴 수 있다.

## L130 | 작업방식 | 2026-08-04 | 압축·워크트리 삭제 후 재개할 땐 "내가 하려던 일이 이미 병합됐는지" 원격에서 먼저 확인하라

- **트리거:** 압축, compact, 이어서, 계속, 워크트리 삭제, 세션 재개, 미완성 작업, 재구현, 다시 만들기, 기계화, 가드 추가
- **실수:** 압축으로 잘린 컨텍스트에 "hwpx 표 폭·secPr·pageBreak 가드를 만들던 중"이라는 기억만 남아, 그대로 재구현하러 들어갔다. 실제로는 작업하던 워크트리가 이미 삭제됐고, 그 사이 다른 세션이 **L086·L087·L088·L089·L091 다섯 건을 이미 기계화해 master 에 병합**해 둔 상태였다. `__all__` 을 눈으로 보고서야 `table_width_from_colspan1` 이 이미 있다는 걸 알았다. 게다가 커버리지 미분류 23건을 등록하는 동안 다른 세션이 **같은 등록을 PR #104 로 먼저 병합**해 충돌까지 났다.
- **재발방지:** 재개 직후 착수 **전에** 3줄로 실측한다 — ① `git fetch origin` ② 만들려는 심볼을 `git show origin/master:<파일> | grep -c "<함수명>"` 로 존재 확인 ③ `git worktree list` 로 내 작업트리 생존 확인. 하나라도 이미 있으면 재구현하지 말고 **남은 구멍만** 채운다. 공유 SSOT 파일(lessons_coverage.json 등)은 등록 즉시 커밋·PR·병합해 창을 좁히고, 충돌 시 상대 등록분을 채택한 뒤 내 고유 변경만 다시 얹는다(상대 작업 되돌리기 금지).

## L131 | 작업방식 | 2026-08-04 | RESUME.md "검증된 사실"에 **실행으로 확인 안 한 것**을 적지 마라 — 다음 세션이 그걸 믿고 헛돈다
- **트리거:** RESUME.md, 검증된 사실, 이어가기, 재확인 불필요, .env, API 키, 환경변수 설정돼 있음, 즉시 사용 가능, 전제조건
- **실수:** 이전 세션이 `RESUME.md` 의 **"검증된 사실 (재확인 불필요)"** 항목에 "`.env` 의 `NAVER_CLIENT_ID`/`NAVER_CLIENT_SECRET` 설정돼 있어 공식 검색 API 즉시 사용 가능"이라고 적었는데, 실제로는 **`NAVER_CLIENT_SECRET` 이 빈 문자열**이라 API 모드가 통째로 건너뛰어진다(`.env` 파일 크기 46바이트로 이미 의심할 수 있었음). 키 **이름이 존재하는 것**을 **값이 채워진 것**으로 오판한 전형. 그 결과 다음 세션(이번)이 "즉시 사용 가능"을 믿고 API 모드를 실행했다가 헛턴을 돌았고, 이어가기 계획("배달의민족 상품권 조회")도 통째로 막혀 있었다.
- **재발방지:** ①"검증된 사실 / 재확인 불필요"에는 **그 세션에서 실제로 실행해 결과를 본 것만** 적는다. 안 돌려본 건 "미검증(추정)"으로 명시 표기. ②환경변수·키·토큰은 **이름 존재 ≠ 값 존재** — `if ($v) {"SET(len=N)"} else {"EMPTY"}` 처럼 **값 유무를 출력**해 확인하고(값 자체는 절대 출력 금지), 그 결과를 근거로 적는다. ③앱을 돌리기 전에 **전제조건(키·네트워크·로그인)부터 1줄로 점검**하면 헛실행 한 사이클을 아낀다.

## L132 | 작업방식 | 2026-08-04 | 공유 누적 파일의 중복 확인은 "저장 전 1회"로 부족 — 저장 직후 재확인까지가 한 세트
- **트리거:** SESSION_RECAP, 회고 저장, RESUME.md, lessons.md, 누적 파일, 중복 저장, prepend, 동시 세션, 다중 세션, 맨 위에 추가
- **실수:** 직전 세션 회고를 쓰기 전 헤딩을 전수 확인해 "중복 없음"을 확정하고 저장했는데, 10분 뒤 다시 보니 그 사이 **다른 세션이 같은 파일에 자기 회고를 끼워 넣어** 있었다(10:45 확인 → 저장 → 10:55 재확인에서 발견). 저장 전 확인만 믿었다면 다음 턴에 또 중복을 만들었을 것이다. 같은 이유로 이 `lessons.md` 도 동시 세션이 같은 번호를 먼저 쓴 전례가 있다.
- **재발방지:** 공유 누적 파일(`SESSION_RECAP.md`·`RESUME.md`·`lessons.md`)은 **① 저장 전 확인 → ② 저장 → ③ 저장 직후 재확인** 3단계로 다룬다. 재확인에서 남의 항목이 나오면 **지우지 말고** 그대로 두고, 나와 같은 작업을 같은 관점으로 쓴 경우에만 합친다. 시각 대조 시 원본 기록(`.jsonl`·tail)은 **UTC**, 회고 헤더는 **KST(+9h)** 임을 반드시 보정한다(`T01:42` = `10:42` 로 같은 작업). 절차 정본: `~/.claude/skills/omc-learned/resume-item-status-update.md` 절차 C.

## L133 | 도구사용 | 2026-08-04 | Bash `ls 'D:\경로\.폴더'` 는 **오류 없이 빈 결과**를 낸다 — 이걸 "폴더 없음"으로 사용자에게 보고하지 마라
- **트리거:** ls, 폴더 확인, 경로 존재 확인, 빈 결과, 결과 없음, 백슬래시, Windows 경로, Git Bash, `@경로` 참조, "폴더가 없습니다"
- **실수:** 사용자가 `@.agents\ kimi` 로 참조하자 `ls 'D:\mail\.agents'` 를 실행했는데 **에러도 없이 출력이 0줄**로 나왔다. 이를 "폴더가 없다"고 단정해 사용자에게 2턴 연속 "경로가 존재하지 않습니다"라고 잘못 보고했다. 실제로는 Git Bash 가 백슬래시를 escape 로 먹어 `Dmail.agents` 라는 엉뚱한 상대경로를 본 것이고, `ls -la D:/mail` 로 보니 `.agents` 폴더는 **멀쩡히 존재**했다(마침 내용이 비어 있어 오판이 더 굳어졌다). 사용자 요청 2번을 헛돌렸다.
- **재발방지:** ①Bash 도구에서 Windows 경로는 **항상 슬래시**(`D:/mail/.agents`)로 쓴다 — 홑따옴표로 감싸도 백슬래시는 안전하지 않다(L112·L069 와 같은 뿌리). ②존재 확인은 `ls <path>` 의 출력 유무가 아니라 **`ls -d <path>` 의 종료코드**나 `test -e` 로 판정한다 — **빈 출력 ≠ 없음**(빈 폴더도 0줄이다). ③"없다/안 된다"를 사용자에게 보고하기 전에 **부모 디렉터리를 한 번 나열**해 교차 확인한다(`ls -la D:/mail | grep agents`). 도구가 조용히 실패했을 가능성을 항상 1순위 가설로 둔다.

## L134 | 디버깅 | 2026-08-04 | Node 기반 CLI가 특정 repo 에서만 `EEXIST: mkdir '.git/info'` 로 죽으면 — 버그가 아니라 **폴더 ReadOnly 속성**이다
- **트리거:** EEXIST, mkdir, .git/info, 특정 폴더에서만 실행 실패, Node CLI, mimo, opencode, ReadOnly, d-r---, 다른 repo 는 되는데
- **실수:** `mimo run` 이 `D:\mail` 에서만 `EEXIST: file already exists, mkdir 'D:\mail\.git\info'` 로 즉사했다. 처음엔 "mimo 0.1.9 버그(모든 git repo 에서 깨짐)"로 단정하고 업그레이드를 시도했다(이미 최신이라 헛수고). **새 임시 git repo 에서 돌려보니 정상 동작** — 즉 도구 버그가 아니라 그 repo 만의 상태 문제였다.
- **재발방지:** ①"이 도구가 깨졌다"고 단정하기 전에 **깨끗한 새 폴더/repo 에서 1회 재현**해 도구 문제 vs 환경 문제를 먼저 가른다(비용 30초, 오진 방지 효과 큼). ②Windows 에서 원인은 대개 **디렉터리 ReadOnly 속성** — `Get-Item <path> -Force` 의 `Mode` 가 `d-r---`(정상은 `d-----`)이면 그것이다. Node 의 `fs.mkdir(recursive:true)` 는 이 경우 EEXIST 를 던진다. 해제: `$i=Get-Item <path> -Force; $i.Attributes = $i.Attributes -band (-bnot [IO.FileAttributes]::ReadOnly)`. ③해제 후 `git status` 로 저장소 무결성을 확인한다(정상 동작 확인됨 — 디렉터리 ReadOnly 는 Windows 에서 사실상 무의미한 속성이라 git 에 영향 없음). ④백신·클라우드 동기화·`attrib +R` 실행 등이 이 속성을 다시 걸 수 있으니 재발하면 같은 곳을 먼저 본다.

## L135 | 작업방식 | 2026-08-04 | 사용자가 "이런 게 자꾸 떠"라고 하면 — 추측으로 단정하지 말고 **내가 방금 띄운 것**부터 의심하고 화면을 직접 확인하라
- **트리거:** 이런 게 떠, 왜 자꾸, 팝업, 창이 떠, 화면, 지시대명사(이거/저거/이런 거), 캡처, 클립보드, 붙여넣기 안 됨, ㄴㄴ, 아니
- **실수:** 사용자가 "왜 자꾸 이런 게 뜨는 거임"이라 했을 때, 화면을 확인하지도 않고 **"세션 가드 경고겠죠"라고 단정**해 답했다 → `아니`/`ㄴㄴ`. 실제 원인은 **내가 hwpx→hwp 변환하려고 COM 으로 띄운 한글 창**(+ 병행 중인 다른 세션의 한글 스크립트)이었다. 내가 방금 실행한 명령이 GUI 를 띄운다는 사실을 스스로 잊고 엉뚱한 곳을 짚어 2턴을 헛돌렸다.
- **재발방지:** ①**직전 내 도구 호출부터 되짚는다** — COM(`HWPFrame.HwpObject`)·`Start-Process`·브라우저 자동화 등 **창을 띄우는 동작**을 내가 했는지 먼저 자문한다(가장 흔한 정답). ②말로 추측하지 말고 **화면을 직접 본다**: 사용자가 캡처를 붙여넣지 못해도 클립보드에서 꺼낼 수 있다 — `powershell.exe -STA -Command "Add-Type -AssemblyName System.Windows.Forms,System.Drawing; $i=[Windows.Forms.Clipboard]::GetImage(); $i.Save('$env:TEMP\clip.png',[Drawing.Imaging.ImageFormat]::Png)"` 로 저장한 뒤 Read 로 읽는다(사용자에게 "붙여넣기 되나요" 되묻지 말 것). ③그래도 모호하면 후보를 2~4개로 좁혀 **1클릭 선택지**로 묻는다. ④GUI 를 띄우는 자동화를 돌린 뒤에는 **프로세스가 남았는지 확인·정리**하고(`Get-Process Hwp*`), 그 사실을 사용자에게 미리 알린다.

## L136 | 진단 | 2026-08-04 | 검사 도구가 "정상"이라 해도 그건 **검사한 항목만** 정상이다 — 도구 출력 문구를 그대로 사용자에게 단정하지 마라
- **트리거:** 진단, 검사 통과, 정상, OK, 판정, 게이트 통과, hwpx_doctor, "이제 열립니다", "이제 됩니다", 검증 완료, 수정 완료
- **실수:** `hwpx_doctor.py repair` 가 `→ ✅ 의미 규칙 전부 통과 — 한글에서 열립니다` 를 출력하자 그대로 믿고 사용자에게 **"이제 열립니다"**라고 보고했다. 실제로 그 도구가 본 것은 표 격자·itemCnt·ID참조 **3항목뿐**이었고, 진짜 원인(한글 2024 전용 `textDir` 속성)은 검사 범위 밖이라 파일은 여전히 안 열렸다. 사용자의 "안열려" 한 마디로 뒤집혔고, 그 전에 "사업계획서 4종 전부 정상"이라 한 것도 격자만 본 결과여서 같이 틀렸다(STAR 도 같은 병).
- **재발방지:** ①도구가 "정상/OK/통과"를 말하면 **무엇을 검사했는지 항목 목록부터 확인**하고, 사용자에겐 "격자·ID·itemCnt 3항목 통과(그 외 미검사)"처럼 **검사 범위를 붙여** 말한다. ②"열립니다/됩니다/해결됐습니다" 같은 **최종 결과 단정은 그 결과를 실제로 확인한 뒤에만** 쓴다(L073 의 확장 — 예측뿐 아니라 *도구의 결론 문구*도 실측이 아니다). ③도구 출력 문구 자체가 과잉 단정이면 그것도 결함으로 취급해 고친다 — `hwpx_doctor` 의 "한글에서 열립니다"는 "검사 항목 전부 통과"로 바꿔야 한다.

## L137 | 진단 | 2026-08-04 | "왜 이 파일만 안 되지"는 **되는 파일과 나란히 놓고 비교**가 최단경로 — 안 되는 파일만 파헤치지 마라
- **트리거:** 이 파일만 안 열려, 이것만 안 됨, 파일 손상 의심, 구조 검사, 원인 모름, 왜 안 되지, hwpx, docx, xlsx, 특정 파일만 실패
- **실수:** 안 열리는 hwpx **한 개만** 붙잡고 ZIP 구조 → XML 구문 → 표 격자 → 제어문자 → ID참조 → content.hpf 정합을 차례로 검사하며 2턴을 썼다. 전부 "정상"이라 막혔다. 뒤늦게 **열리는 파일**(한글이 직접 저장한 사본, 제출 완료된 은평 사업계획서)과 **태그·속성 집합을 diff** 하자 30초 만에 범인이 드러났다 — `textDir` 속성 49개(한글 2024 전용). 처음부터 대조군을 잡았으면 1턴이면 끝났다.
- **재발방지:** 파일 단위 "이것만 안 됨" 문제는 **①되는 파일을 먼저 확보**한다(같은 앱이 저장한 것·과거 정상본·다른 프로젝트의 동종 파일). **②둘의 요소·속성 집합을 diff** 한다 — `set(re.findall(r'<([a-z]+:[a-zA-Z]+)', xml))` 와 `set(re.findall(r'\s([a-zA-Z:]+)="', xml))` 두 줄이면 충분. **③후보가 나오면 전 파일 상관표**(되는 것 vs 안 되는 것 × 후보 속성)를 만들어 상관관계를 확인한 뒤 결론낸다(1:1 비교는 우연일 수 있다). 단일 파일 심층검사는 **대조군이 아예 없을 때만** 쓴다.

## L138 | 도구 | 2026-08-04 | `mimo db "<쿼리>"` 는 **읽기 전용** — 삭제·VACUUM 은 파이썬 sqlite3 로 직접
- **트리거:** mimo db, mimocode.db, attempt to write a readonly database, DB 다이어트, VACUUM, 세션 삭제, sqlite 쓰기 실패
- **실수:** mimo 의 DB 다이어트를 하면서 `mimo db "DELETE FROM ..."` 로 5문장을 연달아 실행했다가 전부 `attempt to write a readonly database` 로 실패했다. mimocode 의 `db` 서브커맨드는 조회 전용으로 커넥션을 연다(도움말엔 "run a query" 라고만 적혀 있어 쓰기도 되는 줄 알았다). 한 라운드를 통째로 날렸다.
- **재발방지:** ①mimocode DB 를 **수정**할 땐 `python -c "import sqlite3..."` 로 파일(`~/.local\share\mimocode\mimocode.db`)을 직접 연다. 스키마상 session→message→part 가 ON DELETE CASCADE 지만 sqlite 는 `PRAGMA foreign_keys=ON` 이 기본 OFF 이므로 **history_fts→part→message→session→external_import 순으로 명시 삭제**가 안전하다. ②쓰기 전 `tasklist /FI "IMAGENAME eq mimo.exe"` 로 프로세스가 죽어 있는지 확인(WAL 잠금). ③Claude Code 세션 자동복제(`external_import.source='cc'`)는 `MIMOCODE_DISABLE_CLAUDE_IMPORT=1` 환경변수로만 끌 수 있고, 끄기 전에 지우면 다음 실행에서 즉시 되살아난다 — **끄기 → 지우기 → VACUUM** 순서를 지킨다. (실측: 287MB→34.6MB)

## L140 | 작업방식 | 2026-08-05 | Bash 도구는 명령 끝나면 cwd 가 원위치로 되돌아간다 — "옮겼다"고 단정하기 전에 이 사실을 먼저 알려라
- **트리거:** 위치, 워크트리, 세션, cd, 폴더 이동, 어디서 실행, 옮겨줘, 위치가 안 옮겨진거같은데
- **실수:** 사용자가 "워크트리·세션 D:/mail로 변경"이라 요청했을 때, `cd /d/mail && ...` 로 명령을 실행하고 결과가 잘 나오자 "정상 실행 확인됨 — D:\mail 로 옮겼다"고 답했다. 그런데 Bash 도구 특성상 각 호출이 끝나면 cwd 가 원래 위치(D:\auto_write)로 리셋된다는 사실을 결과에 언급하지 않았고, 실제로 다음 명령들에도 "Shell cwd was reset to D:\auto_write" 가 매번 찍혔다. 사용자가 "안 옮겨진거같은데"라고 지적한 뒤에야 이 캐벗을 설명했다 — 처음부터 말했어야 할 정보였다.
- **재발방지:** ①이 환경(Bash 도구)은 **명령마다 cwd 가 초기화**된다는 것을 전제로 작업한다 — 매 호출에 절대경로/`cd 대상 &&` 를 반복하고, 결과 파일이 실제로 옳은 경로에 만들어졌는지 별도로 확인한다. ②"폴더를 옮겼다/이동했다"고 보고할 땐, **세션 표시상의 "위치"는 클로드를 실행한 폴더(고정)이고 대화 중엔 바뀌지 않는다**는 점과, 그 폴더 자체를 바꾸려면 창을 새로 열거나(`cd 대상; claude`) `/add-dir` 를 써야 한다는 점을 **먼저** 알려준다. ③작업 산출물이 어디 생겼는지(`ls`/`git status`)로 **증거를 먼저 보여주고** "옮겼다"는 결론은 그 다음에 말한다.

## L141 | 작업방식 | 2026-08-05 | 스크립트 원문(raw warning list)을 그대로 결론으로 옮기지 말고, 보고 전에 반드시 실측 재확인 1회를 거쳐라
- **트리거:** 점검 결과, 경고 목록, 몇 건, 트리거 겹침, 감사 결과, 보고 전 확인
- **실수:** skill-auditor 트리거 겹침 점검에서 스크립트가 뽑아준 "8쌍" 목록을 그대로 사용자에게 "8쌍 모두 경계문 없음"이라 보고했다. 그런데 실제 스크립트 로직은 "둘 중 **하나라도** 경계문이 있으면 OK" 인데, 그걸 다시 들여다보지 않고 목록 개수만 보고 결론을 냈다. 같은 응답 안에서 스스로 재확인해 8쌍 중 실제로 경계문이 아예 없는 건 3개뿐임을 발견해 정정했지만, 애초에 보고 전에 확인했으면 정정이 필요 없었다.
- **재발방지:** 자동화 도구가 뽑아준 "문제 목록"을 사람에게 전달하기 전에, **그 항목이 진짜 문제인지 판정 로직 자체를 한 번 더 읽고 표본 1~2개를 손으로 재현**한다(skill-auditor 의 "1.5단계: 보고 전 오탐 반증"과 같은 원칙 — 도구가 다른 경우에도 항상 적용). 개수·목록을 그대로 베껴 보고하지 않는다.

## L139 | 도구 | 2026-08-05 | 저장 스크립트는 **cwd 를 믿지 마라** — 앞선 `cd` 가 남아 엉뚱한 폴더에 새 파일을 만든다
- **트리거:** save_recap.ps1, SESSION_RECAP.md, RESUME.md, 저장 위치, 작업 폴더, cwd, Set-Location, cd, 상대경로 저장, session-recap, closeout
- **실수:** session-recap 저장 시 `save_recap.ps1` 을 `-Target` 없이 호출했다. 그런데 앞 단계에서 `cd /d/.omc/agent-learning` (Bash) 로 옮겨간 작업 폴더가 **PowerShell 세션에도 그대로 남아 있어**, 스크립트가 그 폴더를 기준으로 잡고 `D:\.omc\agent-learning\SESSION_RECAP.md` 라는 **새 파일을 생성**했다. 진짜 목적지 `D:\SESSION_RECAP.md`(기존 454줄 누적본)에는 아무것도 안 들어갔다. `SAVED: <경로>` 출력을 대충 보고 넘어갔으면 회고가 조용히 유실될 뻔했다.
- **재발방지:** ① 파일을 만드는 스크립트는 **항상 `-Target`/절대경로를 명시**한다(기본값=cwd 인 스크립트는 특히). ② 실행 직후 출력된 저장 경로가 **의도한 경로와 같은지 눈으로 대조**한다 — 다르면 즉시 재저장. ③ Bash 의 `cd` 는 같은 세션의 PowerShell 도구에도 이어진다는 전제로, 작업 전 `Get-Location` 을 확인하거나 스크립트 호출 전에 `Set-Location` 으로 되돌린다. ④ 잘못 만든 파일은 지우기 전에 **기존 내용이 없는 신규 파일인지 확인**하고(있으면 보존), 확인되면 작업 폴더 밖으로 옮긴다.

## L144 | 도구사용 | 2026-08-06 | Bash 로 만든 파일을 Python 으로 검증할 때 **경로 표기를 바꿔야 한다** — `/d/...` `/tmp/...` 는 Python 이 모른다 (L112 의 반대방향)
- **트리거:** Bash heredoc, cat > 파일, 파일 생성 후 검증, FileNotFoundError, /d/, /c/, /tmp/, Git Bash 경로, Windows 경로, py -3.11 -c, open(), 저장 확인, truncated escape
- **실수:** `cat > /d/auto_write/...` 로 파일을 만든 뒤 같은 명령에서 `py -3.11 -c "open('/d/auto_write/...')"` 로 확인하려다 **FileNotFoundError**. 파일은 멀쩡히 만들어졌는데 검증만 실패해 순간 "저장 실패"로 오인했다. Git Bash 는 `/d/...` `/tmp/...` 를 알지만 **Python(Windows)은 `D:/...` 만** 안다. 더 나쁜 건, 히어독 안 Python 문자열의 백슬래시가 도구 전달 과정에서 한 겹 줄어 `SyntaxError: truncated \UXXXXXXXX escape` 가 난 것을 **3번 반복**했고, 이 교훈을 기록하는 명령에서만 **2번 더** 걸렸다는 점이다(L112 재범).
- **재발방지:** ①Bash 는 `/d/...`, **Python 인자·문자열은 `D:/...`** 로 쓴다. `/tmp` 도 마찬가지라 임시파일은 `D:/<repo>/_x.tmp` 처럼 **Windows 경로에 만든다**. ②**히어독 안 Python 에는 백슬래시를 아예 쓰지 않는다.** 긴 본문은 `cat > D:/경로/_x.tmp <<'EOF'` 로 **먼저 파일에 쓰고** Python 은 그 파일을 읽어 붙이기만 한다 — 문자열 이스케이프를 통과시키지 않는 것이 핵심. ③"파일이 없다"는 결과는 **파일 부재가 아니라 경로 표기 문제**를 1순위로 의심하고 `ls -la` 로 교차 확인한다(L133 과 같은 뿌리, 반대방향). ④**공유 누적파일은 저장 직후 "내 항목이 있는가" 만 보지 말고 "번호가 유일한가"까지 확인한다** — 실제로 이 교훈이 다른 세션의 L140 과 번호가 겹친 채 하루를 보냈다(L132 보완).

## L143 | 개발 | 2026-08-05 | refresh_token 일회용 블랙리스트 + 미회전 = 세션만료 무한반복
- **트리거:** 세션만료, 무한반복, 로그인, refresh_token, token rotation, 블랙리스트, 401, 인증
- **실수:** 서버 `/v1/auth/refresh` 엔드포인트가 refresh_token을 사용 즉시 블랙리스트에 등록(일회용)하면서도, 응답에 새 refresh_token을 반환하지 않았다. 클라이언트도 refresh 성공 시 access_token만 저장했다. 첫 refresh 성공 후 refresh_token이 죽으면, 두 번째 refresh 시 서버가 블랙리스트 거부 → 401 → 로그아웃 → 재로그인 → 다시 만료 → 무한루프.
- **재발방지:** ①refresh_token을 일회용으로 만들면 **반드시 새 refresh_token도 함께 반환**한다(회전). ②클라이언트는 refresh 성공 시 **access_token + refresh_token 둘 다 저장**한다. ③다중 탭 환경에서는 한 탭이 이미 rotate한 토큰을 다른 탭이 재사용하는 충돌도 고려 — stale refresh 감지 시 최신 토큰으로 재시도.

## L142 | 문서작성 | 2026-08-05 | 그림 크기를 바꿨으면 **가로만 보지 말고 세로까지** 확인하고 보고하라 — orgSz 비율은 못 믿는다
- **트리거:** 그림, 이미지, 서명, 크기, 확대, 축소, resize, mm, 비율, 눌림, hwpx, scaMatrix, orgSz
- **실수:** 달구벌 신청서의 작은 서명(5.7mm)을 15mm 로 키우고 "확대 완료"라고 보고했는데, 실제 결과는 **15.0 x 0.8mm 로 납작하게 눌린** 상태였다. 검증 스크립트가 **가로(width)만 출력**해 통과로 판단했기 때문이다. 원인은 그 양식의 `orgSz` 가 80.4x4.3mm(비율 18.7:1)로 실제 그림(1.27:1)과 어긋나는데 세로를 orgSz 비율로 계산한 것. 그대로 제출했으면 서명이 얼룩처럼 보였을 것이고, 다음 작업(그림 삽입) 검증 중 우연히 발견했다.
- **재발방지:** ①크기를 바꾼 뒤 검증은 **가로 x 세로를 함께 출력**하고 변경 전 비율과 대조한다(한 축만 보는 검증은 검증이 아니다). ②HWPX 그림의 **orgSz 는 신뢰하지 마라** — 표시 크기는 `sz` 이고 양식에 따라 둘의 비율이 다르다. 확대·축소는 **sz(표시) 비율을 유지**하고 scaMatrix 는 가로·세로를 각각 계산한다(정본: `hwpx_pic_insert.resize_hwpx_picture`, 회귀테스트 `test_resize_hwpx_picture_keeps_display_aspect_when_orgsz_wrong`). ③같은 그림이 문서에 여러 번 쓰이면 **정상인 다른 인스턴스의 비율**이 정답지다(서명 4곳 모두 1.25:1). ④수치를 바꾸는 작업은 "바꿨다"가 아니라 **"바꾼 뒤 값이 이렇게 됐다"** 로 보고한다.

## L145 | 문서작성 | 2026-08-07 | hwpx_fill 을 안 쓰고 좌표로 직접 셀을 고치면 linesegarray strip 을 잊는다 — L002 를 알면서도 재발
- **트리거:** hwpx, 글씨겹침, 좌표, _set_cell_text, 직접 채움, 커스텀 스크립트, cellAddr
- **실수:** 제품양산패키지 신청서(루살카)에서 라벨이 중복(대표자/담당자 이름·생년월일 등)이라 `fill_hwpx()`의 identity/line_edits 매칭이 애매할까 봐, `_set_cell_text` 를 직접 불러 (table, tr, colAddr) 좌표로 78곳을 정밀 채우는 커스텀 스크립트를 짰다. L002(linesegarray 제거)를 이미 알고 있었는데도, `fill_hwpx()`/`_fill_section_xml` 이 편집 끝에 자동으로 해 주는 `_strip_linesegarray(root, only_under=edited)` 호출을 스크립트에 안 넣어서 "글씨겹침"이 재발했다. 사용자가 직접 눈으로 보고서야 잡혔다.
- **재발방지:** ①**엔진의 저수준 함수(`_set_cell_text` 등)를 직접 호출할 때는, 그 함수를 감싸는 상위 파이프라인(`fill_hwpx`/`_fill_section_xml`)이 편집 후에 추가로 하는 일이 있는지 먼저 확인한다** — 특히 `_strip_linesegarray`(L002/L074), black 강제(L083) 같은 "편집 뒤 마무리 스텝"은 저수준 함수 자체엔 없고 파이프라인에만 있다. ②가능하면 애초에 저수준 함수를 우회하지 말고, 좌표 기반 편집이 필요해도 **편집한 tc/문단 리스트를 모아뒀다가 마지막에 `_strip_linesegarray(root, only_under=편집목록)`을 직접 호출**한다(L074: 전역 strip 아님, 편집한 곳만). ③커스텀 HWPX 스크립트를 짤 때마다 "L002/L074/L083 세 가지를 반영했는가"를 체크리스트로 확인한다.
