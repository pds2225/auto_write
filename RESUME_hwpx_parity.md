# RESUME — HWPX 파리티 (B 트랙)

> 격리 워크트리 `D:\auto_write-wt-hwpxparity` / 브랜치 `feat/hwpx-parity`. 테스트 `py -3.11`.
> 목표: HWPX 값 채우기 "완벽히 동작". 저작=fable, 검증=Opus. 최종 갱신: 2026-07-05.

## 확정된 방향
- 사용자 요청: HWPX 파리티(B)만. 허브·중복정리(A)는 나중. fable로 개발.
- 코퍼스: `C:\Users\ekth3\OneDrive\바탕 화면\다솜\경영지도사 개인\00. 공고` (516 파일 = .hwp 449 + .hwpx 67, 대부분 공고문).
- 학습 결과(하네스 측정): **체크박스(□) 2,946개 = 지배적 구조**. 인라인 빈칸 0. 표준칸 채움 18/56 양식. → 우선순위 (d) 체크박스 최상.

## 완료 (커밋 d55c0cb)
- **(a) 셀 인라인 빈칸 채우기** — `_splice_run_text`(run보존·cross-run skip)·`_inline_texts`(중첩표 제외)·used_keys 공유. 검증: 704 passed(회귀 0).
- **tools/hwpx_coverage.py** — .hwpx 커버리지 측정(읽기전용).
- **tools/hwp_to_hwpx_batch.py** — .hwp 양식→.hwpx 변환(한글 COM, _convert_via_com 재사용).

## 완료 (추가)
- **(d) 체크박스 □→■** — 정확일치 1개만·모호 스킵·부분문자열 금지·멱등·used_keys 공유.
  실양식 E2E(□유□무→□유■무 1글자만). 검증 130 passed(회귀 0). 커밋 `3d30180`. 원격 push 완료.

## 완료 (추가 2)
- **(b) 표 밖 본문 단락 빈칸** — 공통 커널 _fill_inline_fields_in_p(1.5·1.8 공유),
  hs:sec 직계만·가시빈칸만·산문 3중 차단. 검증 135 passed(회귀 0). 커밋 `75c6bf0`·push 완료.
- 엔진 채움 경로 4종 완성: 표(1)·셀 인라인(1.5)·체크박스(1.7)·본문(1.8), used_keys 공유.

## 코퍼스 측정 결과 (2026-07-05 확정)
- 변환: 양식 .hwp **133/133 성공(실패 0)** → `_corpus_hwpx\`.
- 재측정: **107/127 양식(84%)에 표준 기업정보 자동 채움, 864 필드, 오류 0**.
- A/B(구엔진 vs 신엔진, 같은 코퍼스): 파일 단위 채움 집합 **완전 동일 = 회귀 0**.
  (앞서 보인 ±2 는 집계 범위 차이 — form 127 vs 전체 133 합산.)
- 신경로 기여: 이 코퍼스는 인라인 빈칸 0·체크박스는 실값 필요(sentinel 정확일치 불가)
  → sentinel 지표엔 0 이 정상. 실값 E2E(□유□무→□유■무)로 기능 증명 완료.
- **0채움 20양식 = 서술형 PSST 사업계획서/확인서** — 기업정보 라벨칸 총 18개뿐:
  우측빈칸 0 / **세로형(아래빈) 4** / 나머지 14는 값칸에 안내문 등 실텍스트(보호=날조0 정상).
- **AC6 판정: (c) 세로 라벨 보류 확정** — 코퍼스 수요 4칸(전체 0.5% 미만)으로 근거 부족.
  matrix-header 오채움 위험 > recall 이득. 코퍼스가 요구하면 재개.

## 다음 액션
1. **B② hwpx_acceptance 게이트 배선** (검출 코드 완성돼 있음 → CLI·게이트 연결).
2. **B③ 제출 오케 HWPX 경로** (submission_orchestrator).
3. B 완료 후 → 보류된 A(허브 오케 통합·중복 정리).

## 안전 불변
원본 미수정·날조0·오매칭<빈칸<덮어쓰기·결정론·양식 100% 보존. 계획 정본: `workspace/PLAN_hwpx_fill_hardening.md`(v3.1).
