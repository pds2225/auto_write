# 🌙 야간 자동개발 결과 보고 (NIGHT_REPORT)

> 자는 동안 이 격리 복사본에서 스스로 점검·개선한 내용을 비개발자 눈높이로 쌓아 둡니다.
> 아침에 읽고 좋은 변경만 진짜 저장소에 반영하면 됩니다. (최신 항목이 맨 위)

---

## 2026-07-02 밤 사이클 (추가 트랙 — 진단이 "무엇을 어떻게 고칠지"까지 알려 줍니다)

### 제출 진단 결과에 "다음에 뭘 하면 제출 가능한지"가 결함마다 붙습니다

- **전에는:** 제출 직전 자가진단(`self_diagnose.py`)이 "자기삽입 안내블록 잔존 2",
  "유색 텍스트 잔존 46" 처럼 **문제 개수만** 보여 줬습니다. 비개발자 입장에선
  이게 무슨 뜻인지, 내가 직접 고쳐야 하는지, 프로그램이 자동으로 고쳐 주는지
  알 수가 없어서 결국 사람에게 물어봐야 했습니다.
- **이제는:** 진단 맨 끝에 **"제출까지 남은 일 (결함별 다음 행동)"** 이 나오고,
  각 문제를 세 갈래로 나눠 **정확히 무엇을 하라고** 알려 줍니다.
  1. **[자동] 명령 한 줄로 해결** — 예: NotebookLM 작업 흔적이 남았으면
     `$ python strip_notebooklm.py "문서.docx"`, 색 글씨가 남았으면
     `$ python auto_write_autopilot.py "문서.docx"` 처럼 **바로 복사해서 붙여넣을
     명령**을 문서 경로까지 넣어 만들어 줍니다.
  2. **[입력] 사람이 직접 채워야 함** — 예: `[확인필요]` 표시 칸, 빈 필수칸은
     "실제 값을 직접 입력하세요. **없는 값을 지어내면 안 됩니다**" 라고 안내합니다(날조 0).
  3. **[수동] 한글/워드에서 직접 손봄** — 예: 실명 마스킹, 빈 표 행 정리 등
     사람이 판단해야 하는 것.
  - 맨 아래에는 **중복 없는 "지금 실행하면 자동 해결되는 명령" 묶음**을 따로 모아
    복붙하기 쉽게 정리해 줍니다.
- **기계도 읽을 수 있게:** `--json` 으로 저장하면 결과에 `remediation` 목록이 함께
  담겨(각 항목의 종류·행동·실행 명령), 다른 자동화·에이전트가 그대로 이어받아
  쓸 수 있습니다.
- **안전:** 진단은 **읽기 전용**이라 문서를 절대 건드리지 않습니다. 값이 없는 칸은
  지어내라고 하지 않고 "사람이 입력" 으로 정직하게 분류합니다.

### 테스트 결과 (증거)

- 새 테스트 **10개 전부 통과** — "16개 검사 전부에 전용 안내가 있다(새 검사가
  생기면 자동으로 실패해 안내 추가를 강제)", "자동/입력/수동 분류가 맞다",
  "명령에 실제 문서 경로가 들어간다", 그리고 **실제 결함 문서를 진단해 화면 출력과
  JSON 에 안내가 실제로 나온다**(끝까지 동작)를 증명.
- 전체 회귀 **554개 모두 통과**(기존 544 + 신규 10, **회귀 0**). 실행:
  `cd app` 후 `py -3.11 -m pytest` (약 2분 13초).
- 실제 결함 문서(마커·작업블록·빈 필수칸·파란 글씨)로 CLI 를 직접 돌려 화면에
  `[자동]`/`[입력]` 분류와 복붙 명령이 정확히 뜨는 것을 확인.

### 건너뛴 것 / 다음 후보

- **임시 확인용 파일 3개**(`app/_e2e_make.py`·`_e2e_defect.docx`·`_e2e_out.json`)를
  스모크 테스트용으로 만들었는데, 야간 규칙상 파일 삭제가 막혀 남아 있습니다.
  **아침에 지워도 무방**합니다(커밋에는 포함 안 됨).
- **다음 후보:** ① 일괄 채움(batch)·autopilot 리포트에도 이 "다음 행동" 안내를
  붙여 한 화면에서 남은 일을 보여주기. ② `unchecked_choices`(선택란)에 완성본이
  있으면 자동 체크 명령을 `[자동]` 으로 승격. ③ 요구사항 원장 미달 항목(R1·R2 등)도
  같은 방식의 "다음 행동" 으로 안내.

---

## 2026-07-02 밤 사이클

### 1) 공고 폴더를 통째로 넣으면 양식이 한 번에 채워집니다 (일괄 채움 + 폴더 분석)

- **전에는:** 양식 파일을 한 개씩 열고, "완성본 A"를 하나하나 지정해서 수동으로 채워야 했습니다.
  또 폴더에 공고문·양식이 섞여 있으면 어떤 게 채울 양식인지 사람이 골라야 했습니다.
- **이제는:** 공고 첨부가 모인 **폴더 하나만 지정**하면
  1. 폴더 안의 양식 파일(.docx/.hwp/.hwpx)을 자동으로 찾아내고(공고문·포스터·안내문은 알아서 제외),
  2. 완성본 모음 폴더에서 **각 양식에 가장 잘 맞는 완성본을 자동으로 골라**,
  3. 값을 옮겨 채운 뒤, 채운 파일을 **한글(.hwp)로 자동 변환**까지 하고(한글 프로그램이 없으면 DOCX만 저장하고 정직하게 안내),
  4. "양식 3개 채움 · 2칸 확인필요 · HWP 2개 생성" 같은 **한국어 요약**을 보여 줍니다.
- 덤으로 `공고 폴더 분석` 기능도 추가됐습니다 — 공고문 마감·지원자격·평가기준과 양식별 작성항목·PSST 영역을 한 번에 정리해 줍니다.
- **사용법(참고):**
  - 일괄 채움: `python cross_form_fill.py batch --notice-folder "<공고폴더>" --source-pool "<완성본폴더>"`
  - 폴더 분석: `python analyze_docs.py folder "<공고폴더>"`
- **안전:** 문서에 없는 값은 지어내지 않고 빈칸/[확인필요]로 둡니다. 원본은 절대 덮어쓰지 않습니다.

### 2) 한글(HWP)로 만든 정부양식에서도 "폰트 검사"가 실제로 작동합니다

- **전에는:** 제출본에 폰트가 여러 종류 섞였는지(혼용) 걸러 주는 검사가 있었지만,
  **한글 파일을 워드(DOCX)로 변환한 정부양식**에서는 폰트 정보가 글자마다 박혀 있지 않고
  "문서 기본 서식"이나 "테마"에만 숨어 있어서, 검사가 "폰트 0종"으로 보고 **아무 문제 없이 통과**시켜 버렸습니다.
  = 정작 가장 흔한 정부양식(한글 변환본)에서 폰트 검사가 사실상 죽어 있었습니다.
- **이제는:** 글자 → 문단 서식 → 문서 기본 서식 → 테마까지 **끝까지 따라가서** 실제 폰트를 알아냅니다.
  그래서 변환된 정부양식에서도 폰트가 뒤섞였는지, 글자 크기가 이상한지 **제대로 검출**합니다.
- **안전:** 글자에 폰트가 이미 지정돼 있던 정상 문서는 동작이 그대로라 **멀쩡한 문서를 잘못 막는 일(오탐)은 없습니다.**
  테마 정보를 못 읽으면 조용히 예전 방식으로 돌아갑니다.

### 테스트 결과

- 폰트 검사(usage_acceptance) 테스트 **57개 통과**(새 검증 4개 포함 — "테마로만 지정된 문서에서 폰트 혼용을 잡는다"를 실제 문서로 증명).
- 전체 테스트 **518개 모두 통과**(회귀 0 — 기존 기능 하나도 안 깨졌습니다). 실행: `cd app` 후 `py -3.11 -m pytest`.

### 건너뛴 것 / 다음 후보

- 테마의 **동아시아(한글) 폰트가 빈 값**인 기본 템플릿에선 ea 슬롯이 안 잡히는 게 정상입니다(오탐 방지). 실제 한글 테마(`script="Hang"`)는 읽도록 구현해 뒀습니다.
- **다음 후보:** ① 일괄 채움 결과의 제출가능성 게이트(usage_acceptance) 자동 연결 — 지금은 채움까지만, 제출가능 판정은 별도 실행. ② R14의 선택란/그림칸 경고를 실제 차단(fail)으로 승격(오탐 코퍼스 확보 후). ③ 폴더 분석에 "어떤 완성본으로 어떤 양식을 채우면 되는지" 추천 매핑 표.

---

## 2026-07-02 밤 사이클 (추가 트랙 — 순수함수 안전망 121개)

> 위 항목과 **다른 세션이 동시에 작업한 별도 트랙**입니다. 기존 코드는 한 줄도 바꾸지 않고,
> 시험(테스트)이 하나도 없던 "순수 계산 부품"에 안전망만 새로 씌웠습니다. (고장 위험 0)

### 무엇을 했나 (쉬운 말)

- **전에는:** 문서 점수 계산·URL 해독·파일명 정리·평가 집계 같은 작은 부품들이
  "지금은 맞게 동작하지만, 나중에 코드를 고치다 실수로 망가져도 아무도 모르는" 상태였습니다.
- **이제는:** 이 부품들을 잘못 건드리면 **테스트가 즉시 빨간불**로 알려 줍니다. 실수로 인한
  고장을 조기에 잡을 수 있게 됐습니다. 특히 **파일명 보안 부품**(`../` 경로 해킹·Windows
  예약어 차단)에도 처음으로 시험이 생겼습니다.

### 지켜보게 된 부품 (테스트 121개)

| 대상 모듈 | 부품 | 개수 |
|---|---|---|
| conversion_fidelity | 변환 일치도 계산 (_norm_text·_ratio·_note_loss) | 14 |
| psst_check | PSST 충실도 등급 판정 (_grade: 누락/미흡/적정/우수) | 11 |
| infographic_suggest | 그림제안 문구 생성·앵커 보정 (3종) | 16 |
| evidence_service | 정부·공공기관 링크 우대·검색 URL 해독 (3종) | 15 |
| submittable_filler | 라벨 정리·더미값(OOO·00백만원) 탐지 (_norm·_key·RESIDUAL_RE) | 13 |
| doc_quality_ops | 문단 서식 통일 시 지배값 고르기 (_dominant) | 8 |
| evaluation_service | 배점추출·점수집계·취약항목·보완지시·직렬화 (5종) | 15 |
| utils | 파일명 보안·슬러그·콘솔치환·중복줄 제거 (4종) | 26 |
| **합계** | | **121** |

### 테스트 결과 (증거)

- 새 테스트 **121개 전부 통과**(모듈별 단독 실행으로 각각 확인).
- 전체 회귀 **544개 통과·실패 0** — `cd app` 후 `py -3.11 -m pytest` (약 2분 15초). 기존 코드
  무수정이라 회귀 위험 없음.
- 남긴 커밋 3개: 순수함수 안전망 80개 / evaluation_service 15개 / utils 26개.
  (새 테스트 파일 7개만 추가.)

### 건너뛴 것 (이유)

- **HWP/워드 COM 실제 실행·문서 생성·무거운 E2E:** 밤 시간·환경 위험으로 제외(세션 지침).
- **`announcement_analyzer`·`folder_analyzer`의 순수함수:** 좋은 후보였지만 **다른 세션이
  그 파일들을 편집 중**이라 시험이 곧 어긋날 수 있어 이번엔 제외(안정된 파일만 골라 작업).
- **파일 읽기/쓰기·네트워크가 섞인 함수:** "순수 계산"이 아니라 제외.
