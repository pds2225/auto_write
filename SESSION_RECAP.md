## 🧾 세션 회고 — 2026-06-30 (작업 본체는 2026-06-24 cross-form)
**주제:** 빈 양식 자동완성(cross-form) — 선택칸 자동체크·하네스 전역화·인천 hwp 결과물 진단

### ✅ 한 일
- 전에는 빈 양식의 `□ 개인 / □ 법인` 같은 선택칸을 사람이 직접 체크해야 했는데, 이제는 완성본의 값(예: "개인사업자")을 읽어 `■ 개인`으로 자동 체크된다(잘못 찍는 경우는 적대검증으로 3중 차단).
- 전에는 빈 양식 자동완성이 D 드라이브 폴더에서만, 매번 명령으로 돌려야 했는데, 이제는 **어느 폴더에서 Claude를 켜도** "빈 양식 채워줘"·"기존 사업계획서로 새 양식 작성" 한마디로 작동한다(전역 등록). 그 말이 다른 기능과 안 헷갈리는지 10개 표현으로 점검해 100% 정확 확인.
- 전에는 어제 인천시 hwp 자동작성 결과물이 왜 절반만·엉망으로 채워졌는지 몰랐는데, 이제 원인이 규명됐다 — ①소스로 준 "최종본"이 표 없는 줄글(서술형)이라 옮길 표가 없었고 ②한글→워드 자동변환에서 표 칸이 밀려 잘못 채워졌다(엔진이 아니라 입력·변환이 문제).

### 🧭 정한 것
- "사실 재배열 전사"(정해진 값을 칸에 그대로 옮김)를 먼저 완성하고, "서술 문장 새로 쓰기"는 다음 단계 하네스로 분리(이번 하네스가 표시한 `[작성 필요]` 칸이 그 작업 목록).
- 체크 표시 기호는 `■`(검은 사각형 — 어느 양식에서나 안 깨짐).
- 인천 hwp를 제대로 채우려면: 한글에서 소스를 `.docx`로 저장(표 정확) 또는 한글 COM으로 hwp에 직접 값만 채움(양식 100% 보존).

### 📂 손댄 파일 (기록 기준)
- `app/auto_write/services/cross_form_autofill.py`·`app/cross_form_fill.py` — 선택칸(체크박스) 자동체크 + 적대검증 보수화(정확일치+정규화 사전).
- `.claude/agents/cross-form-filler.md`·`.claude/skills/cross-form-submission/SKILL.md`·`CLAUDE.md` — 빈 양식 자동완성·제출완성 하네스 신규.
- `~/.claude/skills/cross-form-submission/`·`~/.claude/agents/cross-form-filler.md`·전역 `CLAUDE.md` — 전역화(어느 폴더서나 트리거).

### ⏭️ 다음 할 일
- (이 세션은 이후 6/30 "지원사업 폴더 7개 전수검토 → 진행할 신규사업 없음"으로 이어졌고, 그 상태는 RESUME.md 상단에 별도 기록됨 — 이 회고 범위 밖.)
- cross-form 후속(보류): 서술 문장작성 2단계 하네스(사용자 승인 시).

---

## 🧾 세션 회고 — 2026-06-30
**주제:** 비앤코 인터내셔날로 인천시 해외 온라인쇼핑몰 판매대행 신청서(빈 HWP 양식) 자동 채우기

### ✅ 한 일
- 전에는 "이 공고 신청서를 전에 쓴 적 있나" 알 수 없었는데, 이제 프로젝트 전체를 뒤져 **작성한 적 없음을 확인**했다.
- 전에는 인천시 신청서가 텅 빈 양식이었는데, 이제 **비앤코 회사정보·대표자·품목 HS코드·인증이 어느 칸에 뭘 넣을지 정리된 기입 가이드**가 생겼다.
- 전에는 비앤코 자료가 사업자등록증·사업계획서·엑셀 등 여러 파일에 흩어져 있었는데, 이제 **신청서 칸에 맞춰 한 표로 모여** 그대로 옮겨 적을 수 있다.
- 전에는 빈 양식이었던 칸 중 8개(대표자·사업자번호·업태·품목·담당자·직위·휴대폰·이메일)가 자동으로 채워졌고, 들어간 값이 **엉뚱한 칸 없이 모두 제 자리에** 들어갔음을 확인했다.
- 회사 자료에 **실제로 있는 값만** 쓰고, 없는 값(작년 매출·수출액 등)은 지어내지 않고 "확인필요"로 정직하게 표시했다.

### 🧭 정한 것
- 신청 회사: **비앤코 인터내셔날**(화장품 제조·수출, 자료 폴더는 밸류업파트너스 OneDrive).
- 처리 방식: "빈 양식 채우기" — 자동 전사 + 사람이 옮길 기입 가이드 병행.
- 최종 제출본: **원본 HWP를 한글에서 열어 직접 입력**하는 방식. (자동 채움 DOCX는 표 모양이 흐트러져 참고용 — 이 환경에서 한글 프로그램이 안 떠서 HWP 자동 변환은 불가)

### 📂 손댄 파일
- `D:\auto_write\WORKS\cross_form_outputs\비앤코_인천신청서_기입가이드.md` — 주 산출물(항목별 입력값+출처)
- `D:\auto_write\WORKS\cross_form_outputs\인천신청서_채움.docx` — 자동 8칸 채움(참고용, 양식 흐트러짐)
- `~/.claude/skills/omc-learned/govt-hwp-submit-fill-not-convert.md` — 다음에도 쓰게 방법 보강
- `.omc/wiki`(비앤코 회사 정보 페이지) — 다른 지원사업 신청에도 재사용할 회사 사실 저장

### ⏭️ 다음 할 일
- ⚠️ **마감 확인 먼저** — 공고상 신청 마감이 2026.6.26 18시인데 오늘이 6/30이라 이미 지났을 수 있다. 추가 모집/연장 여부부터 확인.
- 기입 가이드를 보며 한글에서 원본 HWP에 값 입력.
- "확인필요" 칸 채우기: 영문 상호, 우편번호, 고용인원, **전년도 매출액·수출액**, 사무실 전화, 홈페이지, 희망 쇼핑몰 선택.

---

## 🧾 세션 회고 — 2026-06-30
**주제:** 팀 온보딩 안내서 + auto_write 오케스트레이터 점검·보강 + 원격 제어로 막힌 명령 해결

### ✅ 한 일
- 전에는 새 팀원이 무엇부터 봐야 할지 막막했는데, 이제 `D:\` 작업공간 전체와 첫 작업(auto_write)을 한눈에 보는 온보딩 안내서(ONBOARDING.md)가 생겼다. *(세션 초반, 요약된 부분)*
- 전에는 auto_write 문서품질 자동화 도구가 빠진 기능 없이 끝까지 만들어졌는지 불확실했는데, 이제 전 단계가 실제로 동작하고 점검(11개 자동검사 통과)까지 끝나 "껍데기 없이 완성"임이 확인됐다. *(세션 초반, 요약된 부분)*
- 전에는 기본 파이썬(3.14)에 부품이 없어 `python` 으로 바로 검사가 안 됐는데, 이제 필요한 부품을 깔아 바로 돌아가고, 의미 없던 검사 1개도 진짜로 값을 확인하는 검사로 바꿔 신뢰도가 올라갔다. *(세션 초반, 요약된 부분)*
- 전에는 원격으로 PC를 조종하던 중 `/plugin`·`/remote-control` 이 "원격에서는 못 씀"으로 막혀 왜 그런지 몰랐는데, 이제 원인(원격에선 메뉴를 못 그림·자기 연결을 못 끊게 막은 안전장치)과 해제법(폰/웹 창 닫기 또는 PC 앞에서 실행)을 알게 됐다.
- 전에는 같은 함정을 또 만나면 처음부터 헤맸을 텐데, 이제 그 해결법이 스킬로 저장돼 다음에 같은 메시지가 뜨면 자동으로 떠오른다.

### 🧭 정한 것
- 원격 제어 함정 해결법은 "위키"가 아니라 "스킬(omc-learned)"로 저장 — 환경 함정은 다음에 자동 발동되는 스킬 형태가 더 유용하다고 판단(중복 저장 방지).
- 이어가기 파일(RESUME.md)은 **다른 활성 세션(지원사업 탐색)의 최신 체크포인트**라, 이번 세션이 그것을 덮어쓰지 않기로 함(동시에 5개+ 세션이 같은 폴더에서 작업 중 → 남의 작업 보호 우선).
- 플러그인/원격 제어 설정 변경은 원격에서 하지 않고 PC 앞에서 하기로 — 원격 도중 설정을 바꾸면 연결이 끊길 위험.

### 📂 손댄 파일
- `~/.claude/skills/omc-learned/claude-remote-control-host-only-commands.md` — 원격 제어에서 막히는 명령의 원인·해제법을 스킬로 신규 저장(이번 세션 실제 작업).
- `D:\auto_write\ONBOARDING.md` — 팀 온보딩 안내서 작성. *(세션 초반, 요약됨)*
- `D:\auto_write\app\tests\test_document_quality_harness.py` — 의미 없던 검사 1개를 실제 검증으로 보강. *(세션 초반, 요약됨)*

### ⏭️ 다음 할 일
- 플러그인을 정말 업데이트하려면 **PC 앞 로컬 창**에서 `/plugin install oh-my-claudecode` 한 줄(원격에선 안 됨).
- 부팅 때 원격이 자동으로 켜지는 게 싫으면, PC 앞에서 작업할 때 `settings.json` 의 `remoteControlAtStartup` 끄기(요청 시 도와드림).

---

## 🧾 세션 회고 — 2026-06-30
**주제:** STAR 사업계획서(박다솜) HWPX 제출본 완성 + "제출본 공통 후처리" 전체 양식 공통화

### ✅ 한 일
- 전에는 한글에서 열면 글씨가 겹쳐 뭉쳐 못 읽었는데, 이제 정상으로 풀려 읽힙니다.
- 전에는 본문 글씨가 회색·파랑이었는데, 이제 전부 검정(흰색 제목칸만 보존)입니다.
- 전에는 "라스트 100m"라는 어려운 표현이 곳곳에 있었는데, 이제 "지도와 눈앞의 길이 따로 논다 → 사진·간판·랜드마크로 안내"로 쉽게 바뀌었습니다.
- 전에는 양식 안내문구("작성방법…")와 "내가 봐야 할" 설명(※ 비중원칙·disclaimer)이 본문에 남아 있었는데, 이제 깨끗이 빠지고 실제 계획 내용만 남았습니다.
- 전에는 표가 헤더줄·내용줄 구분 없이 한 모양이었는데, 이제 헤더줄(음영·굵게)과 내용줄(보통)이 구분됩니다.
- 전에는 제목이 "■ 외적 동기 — 긴 설명"이었는데, 이제 "■ (외적 동기)"로 깔끔해졌습니다.
- 전에는 이미지를 어디에 넣을지 막막했는데, 이제 도식 6개를 만들 프롬프트와 넣을 위치가 한 파일에 정리됐습니다.
- 전에는 이 수정들을 STAR 한 양식에만 손으로 고쳤는데, 이제 어떤 양식에도 똑같이 적용되는 "공통 9원칙"으로 코드·스킬·메모리·위키에 박혀 다음 양식부터 자동 적용됩니다.
- 제출 가능 여부를 자동 검사해 "제출가능(결함 0)"까지 확인했습니다.

### 🧭 정한 것
- "라스트 100m" 표현 전면 금지 → 사진·간판·랜드마크 기반 도보 안내 보조 서비스로 재정의.
- 항공우주 연계는 위성영상(건물·출입구·랜드마크 데이터화)을 주축, 위성항법은 기본 위치(보조).
- 본문엔 실제 계획만 — ※ 메타·작성태도 문구(단정하지/날조/과장하지 등) 금지.
- 표는 헤더행/내용행 서식을 양식 기준으로 다르게 유지.
- 결과물은 헷갈리지 않게 STAR 폴더 한 곳에 `_제출본.hwpx`로 모음.

### 📂 손댄 파일
- `…01_STAR-Exploration\STAR_사업계획서_박다솜_제출본.hwpx` — 최종 제출본(공통 후처리 적용)
- `…01_STAR-Exploration\STAR_도식_NotebookLM프롬프트.md` — 도식 6개 프롬프트+위치(새 프레이밍)
- `D:\auto_write\STAR_사업계획서_박다솜_고도화.md` — 본문 기록(라스트 0)
- `app/auto_write/services/hwpx_submission_cleanup.py` — 재사용 공통 후처리(테스트 통과)
- 메모리 `submission-common-postprocess`, 위키 `hwpx-docx.md`·`star-exploration.md`, 스킬 `cross-form-submission`

### ⏭️ 다음 할 일
- (1순위·미반영) 직전 요청: 중요 내용 볼드 + "(제목) 1문장 요약 / 내용" 문단형식 + 개조식(~함·~해결) 적용 — STAR 본문에 아직 안 들어감.
- 한글에서 원본 `STAR_사업계획서_박다솜_고도화.hwpx` 닫으면 `_검정수정본`을 원래 이름으로 합치기.
- `hwpx_submission_cleanup.py`를 master에 병합(현재 워크트리).
- STAR 마감(6/29) 경과 — 제출 여부/다른 사업 진행은 사용자 결정.

---

## 🧾 세션 회고 — 2026-06-30

**주제:** 정부 지원사업 공고 폴더 7개 전수 검토 — 지금 신청 가능한 게 있는지 가려내기

### ✅ 한 일
- 전에는 공고문·양식이 한글파일(HWP/HWPX)·PDF로 묶여 있어 마감·자격을 일일이 열어봐야 했는데, 이제 폴더별로 **마감일·자격·지원금·평가기준이 표로 한눈에** 정리됐어요.
- 전에는 어느 사업이 아직 신청 가능한지 몰랐는데, 이제 **올해 이 폴더 안에선 신규로 넣을 수 있는 사업이 사실상 없다**는 걸 확인했어요(대부분 마감).
- 특히 04 관광벤처는 이미 3월에 끝났고, 05 넥스트로컬만 "오늘 마감"이었지만 자격·지원금이 맞지 않아 정리했어요.
- HWPX 공고문이 글자가 너무 적게 나오던 문제(미리보기만 잡힘)를 해결해, **한글 공고문 본문 전체를 제대로 읽어** 분석할 수 있게 됐어요.

### 🧭 정한 것
- 03 국방기술 창업경진대회 — 안 하기로 함(사용자 결정).
- 04 관광벤처 — 마감 경과로 진행 불가.
- 05 넥스트로컬 40+ — 안 하기로 함(자격: 만 40~65세·서울 거주 / 지원금 규모 작음: 1단계 100만, 우수팀 최대 1,000만).
- 02 올해의 K-스타트업 통합공고 — 올해 12개 리그 거의 마감, 내년 참고용으로만 보관.

### 📂 손댄 파일 (기록·정리용만, 코드 변경 없음)
- `D:\auto_write\RESUME.md` — 폴더 7개 검토 결과·결정 반영
- `~/.claude/skills/omc-learned/hwpx-extract-zip-fallback.md` — HWPX 본문 추출 함정 우회법 스킬화
- `.omc/wiki/2026.md` — "박다솜 지원사업 후보 비교(2026 상반기)" 위키 페이지

### ⏭️ 다음 할 일
- 새 방향 선택 대기: (a) 폴더 밖 새 공고(상반기 마감 안 된 것·하반기) 탐색 / (b) STAR 후속 작업 / (c) 다른 작업.
- 다음 공고 검토 시 1차 필터: 예비창업 여부·사업자 보유·연령·지역 제한.
